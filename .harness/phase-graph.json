{
  "schema_version": "1.0",
  "skill_version": "1.0",
  "phases": [
    {
      "id": "PHASE_001",
      "slug": "schema-and-data-model",
      "status": "completed",
      "depends_on": [],
      "started": "2026-04-19T00:00:00Z",
      "completed": "2026-04-19T01:30:00Z",
      "units": [
        {
          "id": "unit_001",
          "description": "Extend skills/development-harness/schemas/phase-graph.json: add required depends_on, touches_paths (required when parallel_safe: true), and parallel_safe on units; add illustrative example data",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/schemas/phase-graph.json now declares depends_on, touches_paths, parallel_safe on every unit across 2 example phases and 6 units",
            "Example demonstrates: depends_on chain in PHASE_001 (unit_002 depends on unit_001), independent siblings in PHASE_002, parallel_safe:false without touches_paths (unit_001), parallel_safe:true with touches_paths globs (5 other units)",
            "python -m unittest discover skills/development-harness/scripts/tests exits 0 (36/36 tests pass)"
          ]
        },
        {
          "id": "unit_002",
          "description": "Extend skills/development-harness/schemas/state.json: add execution.fleet block (mode, batch_id, per-unit fleet entries) with example",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/schemas/state.json now carries execution.fleet with mode ('idle'/'dispatched'/'merging'), batch_id, and units[]",
            "Per-unit fleet entries carry unit_id, phase_id, worktree_path, branch, status ('running'/'succeeded'/'failed'/'merged'), started_at, ended_at, agent_summary_path, and conflict (null or {paths, strategy_applied})",
            "Example demonstrates a dispatched batch with three units: one succeeded, one running, one failed with conflict metadata",
            "python -m unittest discover skills/development-harness/scripts/tests exits 0 (36/36)"
          ]
        },
        {
          "id": "unit_003",
          "description": "Extend skills/development-harness/schemas/config.json: add execution_mode.parallelism, execution_mode.agent_delegation, execution_mode.versioning blocks",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/schemas/config.json: execution_mode promoted from string to object with location + three sub-blocks",
            "parallelism: enabled=false, max_concurrent_units=3, conflict_strategy=abort_batch, require_touches_paths=true, allow_cross_phase=false",
            "agent_delegation: use_explore_for_research=true, use_code_review_skill_for_phase_review=true, parallel_validation_layers=false",
            "versioning: break_on_schema_bump=true (default matches the clean-code direction chosen for this project)",
            "python -m unittest discover skills/development-harness/scripts/tests exits 0 (36/36)"
          ]
        },
        {
          "id": "unit_004",
          "description": "Register .harness/worktrees/ and .harness/logs/ in skills/development-harness/schemas/manifest.json and add entries to the .gitignore template",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/schemas/manifest.json: added entries for .harness/worktrees/ (transient per-batch worktrees) and .harness/logs/ (per-batch execution logs), both harness-owned directories with notes on their purpose",
            "skills/development-harness/commands/create.md: .gitignore template description updated to include 'worktrees/' and 'logs/' alongside '.invoke-active'",
            ".harness/.gitignore in this workspace also updated with worktrees/, logs/, .parallel-disabled, and .lock entries",
            "python -m unittest discover skills/development-harness/scripts/tests exits 0 (36/36)"
          ]
        },
        {
          "id": "unit_005",
          "description": "Bump SCHEMA_VERSION from 1.0 to 2.0 in skills/development-harness/scripts/harness_utils.py; check_schema_version emits actionable re-create error on mismatch",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/scripts/harness_utils.py: SCHEMA_VERSION constant is now '2.0'",
            "check_schema_version mismatch and missing-version errors now include actionable 'Re-run /create-development-harness' pointer",
            "All five skill schema templates (config, issue, manifest, phase-graph, state) bumped to schema_version '2.0'",
            "test_harness_utils.py extended with test_schema_version_is_v2 and test_check_schema_version_v1_rejected_with_recreate_pointer; wrong-version fixture updated to '1.0'",
            "test_validate_harness.py fixtures updated to reference the SCHEMA_VERSION constant (no more hardcoded '1.0')",
            "python -m unittest discover skills/development-harness/scripts/tests exits 0 (38/38 pass)"
          ]
        },
        {
          "id": "unit_006",
          "description": "Extend skills/development-harness/scripts/validate_harness.py: required-field enforcement (no inference), cycle detection for depends_on, touches_paths path-safety checks, version gate with actionable error; add tests including v1 fixture rejection",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/scripts/validate_harness.py: added REQUIRED_UNIT_KEYS, FLEET_MODE_ENUM, _is_touches_path_safe, _find_cycle, _validate_unit; extended _validate_phase_graph with per-unit field enforcement, referential-integrity checks for phase and unit depends_on, and cycle detection; extended _validate_state with execution.fleet.mode enum validation",
            "Version gate: check_schema_version (called from run_validation) rejects v1 fixtures with the /create-development-harness pointer",
            "test_validate_harness.py: 22 new cases across TestValidateHarnessV2Schema (v1 rejection on all four JSON files, missing depends_on/parallel_safe, parallel_safe without touches_paths, empty touches_paths, .. traversal, POSIX absolute, Windows absolute, phase cycle, unit cycle, unknown phase dep, unknown unit dep, fleet.mode enum, missing fleet.mode, fleet.mode idle accepted, valid v2 unit accepted) + TestPathSafetyHelper (4) + TestFindCycleHelper (6)",
            "python -m unittest discover skills/development-harness/scripts/tests exits 0 (65/65 pass, up from 38)"
          ]
        }
      ]
    },
    {
      "id": "PHASE_002",
      "slug": "frontier-selector-and-batch-computation",
      "status": "completed",
      "depends_on": ["PHASE_001"],
      "started": "2026-04-19T18:15:00Z",
      "completed": "2026-04-19T21:30:00Z",
      "units": [
        {
          "id": "unit_007",
          "description": "Rewrite skills/development-harness/scripts/select_next_unit.py around frontier computation; add --frontier and --max N flags; no legacy list-order fallback; preserve stop-hook JSON contract on no-flag call",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/scripts/select_next_unit.py rewritten around compute_frontier(phases, max_items): deterministic frontier from v2 phase/unit depends_on; MalformedPhaseGraph raised (exit 2, stderr) when a unit is missing depends_on or has non-list depends_on -- no silent legacy fallback",
            "No-flag CLI invocation preserves the v1 stop-hook JSON contract (found/phase_id/phase_slug/unit_id/unit_description/phase_complete/all_complete); phase_complete signal reinstated via _phase_completion_pending",
            "New --frontier flag prints the full frontier as a JSON array with phase_id, phase_slug, id, description, status, depends_on, touches_paths, parallel_safe on each entry; --max N caps the list; ordering is phase-list then unit-list (deterministic)",
            "test_select_next_unit.py fixtures upgraded to v2 shape (every unit carries depends_on + parallel_safe); added TestFrontierFlag (4 cases: all-ready, --max cap, v2 field pass-through, empty-on-all-complete) and TestNoLegacyFallback (2 cases: missing depends_on, non-list depends_on -- both exit 2)",
            "python -m unittest discover skills/development-harness/scripts/tests exits 0 (71/71 pass, up from 65)",
            "Manual smoke: running the new selector against this workspace's v1-shaped phase-graph errors loudly with 'Run validate_harness.py -- the graph is not v2-conformant', confirming the no-fallback stance"
          ]
        },
        {
          "id": "unit_008",
          "description": "Implement frontier resolution strictly from depends_on; error loudly on malformed units; cover linear, diamond, disconnected, partially-completed graphs",
          "status": "completed",
          "validation_evidence": [
            "TestFrontierTopologies (new) covers all four required graph shapes: linear (head-ready, middle-ready-after-head-done), diamond (siblings ready after root, bottom ready after both siblings, bottom blocked while one sibling is in_progress), disconnected (two independent phase subgraphs both surface their heads; independent progress on each), partially-completed (middle-gap chain, parallel-siblings mid-progress)",
            "Phase-complete-pending signal coverage: when an earlier phase has all units done but is not yet marked completed, the no-flag selector surfaces found=False, phase_complete=True, all_complete=False; once the phase is marked completed, the later phase's unit becomes the found head and phase_complete drops to False",
            "Malformed-unit coverage expanded: TestNoLegacyFallback now asserts exit 2 on (a) missing depends_on, (b) non-list depends_on, (c) non-dict unit, (d) missing id. All error paths surface a clear message and direct the user to validate_harness.py",
            "python -m unittest discover skills/development-harness/scripts/tests exits 0 (83/83 pass, up from 71 after unit_007)"
          ]
        },
        {
          "id": "unit_009",
          "description": "New skills/development-harness/scripts/compute_parallel_batch.py with stdlib fnmatch glob-overlap matrix; greedy-pack under max_concurrent_units; emits {batch, excluded, batch_id}",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/scripts/compute_parallel_batch.py: new CLI entry point (--input, --config, --root); compute_batch(frontier, parallelism) runs a deterministic greedy left-to-right pack; _patterns_overlap uses stdlib fnmatch for literal/glob and a literal-prefix containment heuristic for glob/glob (pessimistic: false positives serialise, never corrupt); output schema is {batch_id, batch, excluded} where excluded carries machine-readable reasons",
            "_parallelism_config extracts from execution_mode.parallelism with v1-safe fallback (string execution_mode falls back to defaults); defaults: max_concurrent_units=3, require_touches_paths=true, allow_cross_phase=false, conflict_strategy=abort_batch",
            "batch_id format: batch_YYYY-MM-DDTHH-MM-SSZ (UTC timestamp) via _make_batch_id; test locks the format with a regex and a fixed-datetime assertion",
            "Coverage (new test_compute_parallel_batch.py, 23 cases): TestPatternOverlap (7 cases incl. literal/literal, literal/glob, glob/glob containment, disjoint globs, empty-prefix star, helper regressions), TestParallelismConfigExtraction (3 cases incl. v1 string fallback), TestComputeBatch (8 cases incl. non-overlapping pack, capacity cap, overlap serialisation, parallel_safe false, require_touches_paths true/false, cross-phase deferral vs allow_cross_phase), TestBatchIdFormat (2), TestCliSmoke (2 incl. --help)",
            "python -m unittest discover skills/development-harness/scripts/tests exits 0 (106/106 pass, up from 83 after unit_008)"
          ]
        },
        {
          "id": "unit_010",
          "description": "Machine-readable exclusion reasons ('not_parallel_safe', 'path_overlap_with:<unit_id>', 'capacity_cap'); unit tests asserting each reason fires on a crafted input",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/scripts/tests/test_compute_parallel_batch.py: added TestExclusionReasons with one dedicated test per reason -- test_reason_not_parallel_safe_fires (single parallel_safe=False unit; excluded == [{unit_id:'solo', reason:'not_parallel_safe'}]), test_reason_path_overlap_with_fires (u1=src/shared/**, u2=src/shared/auth.ts; excluded == [{unit_id:'loser', reason:'path_overlap_with:winner'}] -- asserts the <unit_id> suffix), test_reason_capacity_cap_fires (two disjoint units, max_concurrent_units=1; excluded == [{unit_id:'u2', reason:'capacity_cap'}])",
            "Each test uses the narrowest possible crafted input so only the reason under test can fire; test names embed the reason literal so a future refactor that drops a constant surfaces as a named failure",
            "python -m unittest skills.development-harness.scripts.tests.test_compute_parallel_batch -v exits 0 (26/26 pass, up from 23 after unit_009; 3 new TestExclusionReasons cases)",
            "python -m unittest discover skills/development-harness/scripts/tests exits 0 (109/109 pass, up from 106 after unit_009)"
          ]
        }
      ]
    },
    {
      "id": "PHASE_003",
      "slug": "intra-unit-helper-agent-delegation",
      "status": "completed",
      "depends_on": [],
      "started": "2026-04-19T21:45:00Z",
      "completed": "2026-04-19T23:20:00Z",
      "units": [
        {
          "id": "unit_011",
          "description": "Insert Exploration step in skills/development-harness/commands/invoke.md; dispatch Agent(Explore) for refactor/extend/fix/migrate/update keywords before implementation",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/commands/invoke.md: inserted new 'Step 6: Exploration (conditional)' between 'Step 5: Read Phase Context' and the former 'Step 6: Plan the Unit' (now Step 7). The new step lists the five trigger keywords (refactor / extend / fix / migrate / update), a skip rule for from-scratch units (add / new / create / insert / scaffold), a concrete Agent(subagent_type='Explore', thoroughness='medium') call with a reusable prompt template, and guidance on absorbing the agent's report into the Step 7 plan",
            "Steps 7-13 renumbered (old Step 6 Plan -> Step 7, old Step 7 Implement -> Step 8, ..., old Step 12 Turn Ends -> Step 13); subsection labels 9a/9b/9c -> 10a/10b/10c",
            "Three internal cross-references updated to match the new numbering: 'phase completion review (Step 9)' -> '(Step 10)' in Step 4's output interpretation; 'see Step 8' -> 'see Step 9' in Step 7 Plan; 'Step 11 (commit/PR workflow)' -> 'Step 12' and 'Step 9 (phase completion review)' -> 'Step 10' in the Skills Integration block",
            "Grep verification: 'Step 6: Exploration' matches at line 94; the five trigger keywords ('refactor', 'extend', 'fix', 'migrate', 'update') appear on consecutive bullet lines 102-106; 'Agent(' with subagent_type='Explore' appears at line 115",
            "python -m unittest discover skills/development-harness/scripts/tests exits 0 (109/109) -- docs-only change, test suite unchanged"
          ]
        },
        {
          "id": "unit_012",
          "description": "Mirror Exploration step in skills/development-harness/templates/workspace-commands/invoke-development-harness.md",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/templates/workspace-commands/invoke-development-harness.md: inserted new '## 7. Exploration (conditional)' between the existing '## 6. Read Phase Context' and the former '## 7. Plan Internally' (now '## 8. Plan Internally'). The new section mirrors the command-doc (skills/development-harness/commands/invoke.md Step 6) content in the compact template style: same five trigger keywords (refactor/extend/fix/migrate/update), same five skip keywords (add/new/create/insert/scaffold), same Agent(subagent_type='Explore', thoroughness='medium') call template, same guidance on absorbing the report into the plan",
            "Sections 8-14 renumbered: old 7 Plan -> 8, 8 Implement -> 9, 9 Validate -> 10, 10 Update State -> 11, 11 Commit -> 12, 12 Phase Completion Review -> 13, 13 Turn Ends -> 14",
            "Two internal cross-references updated to the new numbering: 'phase completion review (step 12)' -> '(step 13)' on line 58; new section 7 prose references 'Step 8's plan' to match the renumbered Plan Internally section",
            "Grep verification: 'Exploration (conditional)' matches at line 65; the five trigger keywords appear inline on line 69; 'Agent(' appears at line 76; every '## N.' heading appears exactly once in order 0 through 14",
            "Cross-doc consistency check: command doc and template both carry identical trigger keywords, skip keywords, Agent call shape, and 'main agent still does the editing' framing. Template is tighter (compact-section style) but semantically identical",
            "python -m unittest discover skills/development-harness/scripts/tests exits 0 (109/109) -- docs-only unit"
          ]
        },
        {
          "id": "unit_013",
          "description": "Add multi-file parallel-edit guidance (fan out to 2-3 Agent calls in one message when >=4 independent files) to both invoke command docs",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/commands/invoke.md: added '### Multi-file parallel edits' sub-section inside Step 8 (Implement the Unit). States the >=4-independent-files threshold, the single-assistant-message / 2-3 Agent(subagent_type='general-purpose') fan-out shape, why bounding at 3 (coordination overhead above; round-trip cost isn't worth it below), and groups-by-independence vs file-count. Two worked examples: parallel-safe (mirrored template docs) and NOT-parallel-safe (symbol rename with cascading callsites)",
            "skills/development-harness/templates/workspace-commands/invoke-development-harness.md: added a **bold-label** 'Multi-file parallel edits.' paragraph inside section 9 (Implement). Semantically identical to the command-doc version (same threshold, same shape, same grouping rule, same 'below threshold: direct Edit/Write' fallback) but compressed to the template's single-paragraph style",
            "Grep verification: '>=4 independent files' appears in both files; 'single assistant message' appears in both files; '2-3 `Agent' appears in both files; `subagent_type: \"general-purpose\"` appears in both files",
            "Cross-doc consistency: both docs cite the same numeric threshold, the same fan-out shape (single message, 2-3 calls, general-purpose subagent), and the same 'group by independence' rule. Command doc carries worked examples; template stays minimal",
            "python -m unittest discover skills/development-harness/scripts/tests exits 0 (109/109) -- docs-only unit"
          ]
        },
        {
          "id": "unit_014",
          "description": "Add parallel phase-review dispatch guidance (run code-review skill concurrently with commit-agent-changes) in both invoke command docs",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/commands/invoke.md: added 'Parallel dispatch with commit-agent-changes' sub-section at the top of Step 10 (Phase Completion Review). Rule states: when both code-review and commit-agent-changes are installed, dispatch them in a single assistant message containing two Agent(subagent_type='general-purpose') tool calls. Carries a concrete code sample of the two parallel Agent calls, explains why they cannot conflict (one reads, one writes disjoint state), and documents the single-skill-fallback path (serial, no parallel dispatch)",
            "skills/development-harness/commands/invoke.md Step 12 (Commit): added a call-out pointer referencing Step 10's parallel-dispatch sub-section so the agent skips the separate delegation when phase-completion already fan-out the commit work",
            "skills/development-harness/templates/workspace-commands/invoke-development-harness.md: added a 'Parallel dispatch.' bold-label paragraph to section 13 (Phase Completion Review) mirroring the command-doc rule -- same single-assistant-message + two-Agent-call shape, same disjoint-state justification, same single-skill-fallback fallback; section 12 (Commit) gained the same call-out pointer",
            "Grep verification: 'Parallel dispatch' heading + rule appears in both files; both docs cite 'single assistant message' with 'two `Agent(subagent_type: \"general-purpose\")` tool calls' at phase completion; command doc line 224, template line 152",
            "Cross-doc consistency: both docs state the same trigger (both skills installed), the same shape (one message, two Agent calls), the same disjoint-state justification, and the same serial fallback when only one skill is installed",
            "python -m unittest discover skills/development-harness/scripts/tests exits 0 (109/109) -- docs-only unit"
          ]
        },
        {
          "id": "unit_015",
          "description": "Update skills/development-harness/templates/claude-code/rules/harness-core.md and the Cursor mirror with delegate-vs-inline guidance",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/templates/claude-code/rules/harness-core.md: added a new '## Delegation (when to dispatch sub-agents)' section below 'Quality'. Consolidates the three dispatch shapes introduced by PHASE_003: Agent(Explore) before planning when unit description carries refactor/extend/fix/migrate/update; fan-out of 2-3 Agent(general-purpose) calls when >=4 independent files; parallel code-review + commit-agent-changes at phase completion. Each bullet names the Step in commands/invoke.md where the concrete prompt template lives. Section opens with 'Default to inline' and closes with 'Stay inline' fallbacks so the main agent's default remains the direct Edit/Write path",
            "skills/development-harness/templates/rules/harness-core.mdc (Cursor mirror): added the same '## Delegation' section. Block is byte-identical to the Claude Code version; `diff` confirms the only differences between the two files remain the pre-existing frontmatter delimiter (paths: vs globs:) and the rules-directory path (.claude/rules/ vs .cursor/rules/)",
            "Grep verification: '## Delegation (when to dispatch sub-agents)' matches exactly once in each of the two files",
            "python -m unittest discover skills/development-harness/scripts/tests exits 0 (109/109) -- docs-only unit"
          ]
        }
      ]
    },
    {
      "id": "PHASE_004",
      "slug": "parallel-validation-layers",
      "status": "completed",
      "depends_on": [],
      "started": "2026-04-19T23:30:00Z",
      "completed": "2026-04-20T00:15:00Z",
      "units": [
        {
          "id": "unit_016",
          "description": "Rewrite validation step in skills/development-harness/commands/invoke.md to fan out Layer 1 (lint + typecheck + formatter) and Layer 2 (unit tests) as concurrent Bash calls when config.agent_delegation.parallel_validation_layers == true",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/commands/invoke.md: added '### Parallel Layer 1 + Layer 2 (when enabled)' sub-section at the top of Step 9 (Validate), inserted between the 'only run relevant layers' intro and the existing Layer 1 heading. Triggered by config.agent_delegation.parallel_validation_layers == true; shape is explicitly 'single assistant message' containing multiple Bash tool calls for lint/typecheck/unit-tests. Concrete three-Bash-call sample included. Failure semantics: unit passes only if every parallel call exits 0; any failure drops back into the existing On Failure flow with no Layer 3/4 advancement. Layers 3 and 4 kept serial with justification (port/fixture/DB/test-account contention). Flag-off branch documented as default and unchanged from v1",
            "Grep verification: 'Parallel Layer 1 + Layer 2' section header at line 181; 'config.agent_delegation.parallel_validation_layers' and 'single assistant message' both appear at line 183; flag-off preservation text 'The flag-off behavior is unchanged from the v1 flow' present",
            "Acceptance criteria met: doc explicitly says 'single assistant message' + 'multiple Bash tool calls'; behavior when flag is off is preserved (existing Layer 1-4 subsections unchanged; new sub-section says 'skip this block' when flag is false)",
            "python -m unittest discover skills/development-harness/scripts/tests exits 0 (109/109) -- docs-only change, test suite unchanged"
          ]
        },
        {
          "id": "unit_017",
          "description": "Mirror parallel-validation-layer step in skills/development-harness/templates/workspace-commands/invoke-development-harness.md",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/templates/workspace-commands/invoke-development-harness.md: added a '**Parallel Layer 1 + Layer 2 (when enabled).**' bold-label paragraph inside section 10 (Validate), between the four-layer numbered list and the 'On failure' block. Semantically identical to the command-doc unit_016 version -- same trigger (config.agent_delegation.parallel_validation_layers == true), same single-assistant-message + multiple-Bash-calls shape, same all-must-pass failure semantics with no Layer 3/4 advancement on red, same Layers-3-and-4-stay-serial rule with the same port/fixture/DB/test-account justification, same flag-off default preservation",
            "Grep verification: 'Parallel Layer 1' + 'config.agent_delegation.parallel_validation_layers' + 'single assistant message' all appear in section 10 at line 114; byte 'unchanged from v1' is present in the flag-off clause",
            "Cross-doc consistency check: both docs cite the same flag name, the same fan-out shape, the same all-must-pass semantics, the same Layers-3/4-stay-serial rule, and the same flag-off default. Command doc has concrete Bash call sample and heading-level sub-section; template compresses to a single bold-label paragraph matching its compact style",
            "python -m unittest discover skills/development-harness/scripts/tests exits 0 (109/109) -- docs-only change, test suite unchanged"
          ]
        },
        {
          "id": "unit_018",
          "description": "Update evidence-recording format in both invoke command docs to include per-layer timing (e.g., 'pnpm lint exits 0 (2.1s)')",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/commands/invoke.md Step 9 'On Success' block rewritten: updated intro sentence to require per-layer wall-clock timing on every entry; expanded the 'Good' example list with four concrete entries carrying timings ('pnpm lint exits 0 (2.1s)', 'tsc --noEmit exits 0 (4.8s)', 'tests/auth.test.ts passes (5/5 assertions, 3.2s)', 'pytest tests/test_auth.py passes (12/12, 1.9s)'); added a specific 'Bad' example showing 'pnpm lint exits 0' without timing as now-insufficient; appended an explanatory paragraph on how to source the timing (the real line from 'time', or the 'Ran N tests in X.Xs' tail from unittest) and why this makes the parallel-layers benefit visible (max(layer_1, layer_2) vs. sum)",
            "skills/development-harness/templates/workspace-commands/invoke-development-harness.md section 10 'On success' bullet rewritten: single compact bullet requiring 'per-layer wall-clock timing' with three concrete examples ('pnpm lint exits 0 (2.1s)', 'tsc --noEmit exits 0 (4.8s)', 'tests/auth.test.ts passes (5/5, 3.2s)') and the same max-vs-sum rationale compressed inline",
            "Grep verification: the regex 'pnpm lint exits 0 \\(\\d' matches the timing format example in both files (commands/invoke.md line 244; workspace-commands template line 122)",
            "Cross-doc consistency: both docs cite the same concrete examples and the same rationale; command doc carries fuller prose including how to source timings, template stays minimal",
            "python -m unittest discover skills/development-harness/scripts/tests exits 0 (109/109) -- docs-only change"
          ]
        }
      ]
    },
    {
      "id": "PHASE_005",
      "slug": "worktree-dispatch-and-merge-infrastructure",
      "status": "completed",
      "depends_on": ["PHASE_001", "PHASE_002"],
      "started": "2026-04-20T00:35:00Z",
      "completed": "2026-04-20T03:00:00Z",
      "units": [
        {
          "id": "unit_019",
          "description": "New skills/development-harness/scripts/dispatch_batch.py: creates git worktrees under .harness/worktrees/<batch_id>/<unit_id>, creates harness/<batch_id>/<unit_id> branches, seeds WORKTREE_UNIT.json, writes state.execution.fleet; atomic teardown on failure",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/scripts/dispatch_batch.py: new 207-line stdlib-only module. Public API: dispatch_batch(batch_result, root, state=None, now=None) takes the {batch_id, batch, excluded} dict from compute_parallel_batch and per unit runs 'git worktree add -b harness/<batch_id>/<unit_id> .harness/worktrees/<batch_id>/<unit_id> HEAD' (via subprocess), seeds '<worktree>/.harness/WORKTREE_UNIT.json' with batch_id/unit_id/phase_id/touches_paths, and returns the updated fleet block. When a 'state' dict is passed, state.execution.fleet is replaced in place with {mode='dispatched', batch_id, units=[...]} where every entry matches the v2 state schema (status='running', started_at=now, ended_at=null, agent_summary_path=null, conflict=null). Atomic failure semantics: any subprocess.CalledProcessError, OSError, or KeyError raised during per-unit processing triggers _rollback(root, created) which best-effort 'git worktree remove --force' + 'git branch -D' every worktree/branch the call created before re-raising as DispatchError. CLI: --batch <file> --root <dir> --state <file>",
            "skills/development-harness/scripts/tests/test_dispatch_batch.py: new 194-line test module with 9 cases across 3 classes. TestHappyPath: creates worktrees + branches + WORKTREE_UNIT.json contents + fleet-entry shape + empty-batch edge + state-is-written-in-place. TestAtomicRollback: pre-creates a colliding branch on unit_b so the second 'git worktree add' fails and asserts unit_a's worktree + branch are removed after DispatchError (pre-existing unit_b branch untouched); second case exercises the KeyError path by omitting 'id' on the second unit and asserts same rollback shape. TestCliSmoke: end-to-end via subprocess asserting stdout JSON, state.json mutation including last_updated, plus --help",
            "Test fixtures use a real tempdir git repo (git init + user config + one commit) because worktree ops require a non-trivial commit graph. The helper _init_repo(path) is local to the test module; this is the first git-subprocess-using test file in the suite",
            "Validation evidence with per-layer timing (per unit_018 rule): `python -m py_compile skills/development-harness/scripts/dispatch_batch.py skills/development-harness/scripts/tests/test_dispatch_batch.py` exits 0 (0.14s); `python -m unittest skills.development-harness.scripts.tests.test_dispatch_batch -v` passes 9/9 (3.5s); `python -m unittest discover skills/development-harness/scripts/tests` passes 118/118 up from 109 (11.3s wall-clock)"
          ]
        },
        {
          "id": "unit_020",
          "description": "New skills/development-harness/scripts/merge_batch.py: serial fan-in via git merge --no-ff; conflict_strategy=abort_batch or serialize_conflicted; post-merge validation; git reset --hard rollback on failure",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/scripts/merge_batch.py: new 217-line stdlib-only module. Public API: merge_batch(state, root, *, conflict_strategy='abort_batch', run_post_merge_validation=None, now=None). Flips fleet.mode to 'merging', captures pre_merge_ref = git rev-parse HEAD, iterates fleet.units in order running 'git merge --no-ff <branch> -m \"harness: merge <unit_id>\"'. On conflict: 'git merge --abort' always, then conflict_strategy decides -- abort_batch flips the unit's status to 'failed' with conflict={paths, strategy_applied} and flags remaining units as 'failed' (conflict=null, they were skipped); serialize_conflicted leaves the unit status='running' with conflict recorded so its worktree + branch stay alive for a later batch. Post-merge runs run_post_merge_validation(root, merged_unit_ids) (default: _noop_validate that always returns True) and on failure does 'git reset --hard <pre_merge_ref>' then downgrades every previously merged unit to 'failed' with strategy_applied='post_merge_validation_failed'. On success, every merged unit has its worktree ('git worktree remove --force') and branch ('git branch -D') cleaned up. Return value names the outcome: 'ok' | 'partial' | 'aborted' | 'validation_failed' | 'all_conflicted' | 'empty'. CLI: --state --root --conflict-strategy (choices validated at argparse level)",
            "Scope-violation detection is left for unit_022 and .harness/.lock mutex for unit_023 -- both are clean insertion points in merge_batch's pre-per-unit and wrapper layers respectively",
            "skills/development-harness/scripts/tests/test_merge_batch.py: new 340-line test module with 16 cases across 7 classes. TestHappyPath: 5 cases (all clean, commit messages, branches deleted, empty fleet, mode transitions). TestAbortBatchStrategy: 2 cases (abort semantics, branch-cleanup asymmetry -- merged branch deleted, conflicted + skipped branches preserved). TestSerializeConflictedStrategy: 2 cases (conflicted stays running + remaining merge, conflicted branch preserved). TestPostMergeValidation: 3 cases (validator called with correct merged_ids, reset --hard rollback on failure with HEAD verified at pre-merge SHA, validator not called when no unit merged). TestStrategyValidation: unknown strategy raises MergeError. TestEndToEndWithDispatch: full dispatch_batch -> commit in worktree -> merge_batch -> worktrees removed cycle. TestCliSmoke: --help and end-to-end state.json write",
            "Validation evidence with per-layer timing: `python -m py_compile` on both new files exits 0 (0.13s); `python -m unittest skills.development-harness.scripts.tests.test_merge_batch` passes 16/16 (12.0s); `python -m unittest discover skills/development-harness/scripts/tests` passes 134/134 up from 118 (21.6s)"
          ]
        },
        {
          "id": "unit_021",
          "description": "New skills/development-harness/scripts/teardown_batch.py: idempotent cleanup of worktrees and harness/batch_* branches; safe to run under /clear, /sync, and error recovery",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/scripts/teardown_batch.py: new 146-line stdlib-only module. Public API: teardown_batch(root, batch_id=None) -> {removed_worktrees, deleted_branches, batch_ids}. Scoped mode (batch_id='<id>') cleans just that batch; global mode (batch_id=None) cleans every .harness/worktrees/batch_*/ directory and every 'harness/batch_*/' branch. Every git call runs with check=False so missing worktrees / missing branches / already-pruned entries never raise. Orphaned on-disk worktree directories that git no longer knows about are scrubbed via shutil.rmtree after the 'git worktree remove --force' best-effort. Empty batch dirs and the worktrees root are auto-pruned. Final 'git worktree prune' resyncs git's metadata. CLI: --batch-id (default all) --root",
            "skills/development-harness/scripts/tests/test_teardown_batch.py: new 172-line test module with 10 cases across 4 classes. TestScopedTeardown (4): single-batch teardown removes worktrees + branches + dir, second run is a no-op, scoped teardown leaves other batches alone, phantom batch_id is a no-op. TestGlobalTeardown (2): global mode removes every batch + every harness/* branch, clean-repo global teardown is a no-op. TestMissingStateTolerance (2): orphaned branch without worktree still gets deleted and is reported; orphaned on-disk worktree dir without branch still gets scrubbed. TestCliSmoke (2): --help + end-to-end scoped CLI invocation",
            "Idempotence property verified: TestScopedTeardown.test_second_run_is_a_no_op asserts the second call returns empty removed_worktrees + deleted_branches + batch_ids. TestGlobalTeardown.test_global_teardown_on_clean_repo_is_a_no_op asserts the same on a pristine repo with no dispatch history",
            "Validation evidence with per-layer timing: `python -m py_compile` on both new files exits 0 (0.15s); `python -m unittest skills.development-harness.scripts.tests.test_teardown_batch -v` passes 10/10 (4.4s); `python -m unittest discover skills/development-harness/scripts/tests` passes 144/144 up from 134 (27.8s)"
          ]
        },
        {
          "id": "unit_022",
          "description": "Scope-violation detector in merge_batch.py: computes git diff --name-only <merge-base>..<branch> and rejects any unit whose diff exceeds declared touches_paths, regardless of sub-agent self-report",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/scripts/merge_batch.py: added three new helpers -- _is_within_scope(file_path, touches_paths) using fnmatch.fnmatchcase (fnmatch's `*` already matches path separators so `src/auth/**` naturally covers descendants); _scope_violations(root, branch, touches_paths) that runs `git merge-base HEAD <branch>` then `git diff --name-only <merge-base>..<branch>` and returns files matching no declared glob; _read_worktree_touches_paths(root, worktree_path) that reads the WORKTREE_UNIT.json sentinel seeded by dispatch_batch. Integration: the per-unit loop in merge_batch() now calls _scope_violations before _merge_unit. On violation, the unit is rejected with status='failed' and conflict={paths: <violators>, category: 'scope_violation'} -- NO merge is attempted and HEAD does not advance. Scope failures are always hard rejects regardless of conflict_strategy (don't conflate with merge conflicts). Other well-scoped units in the same batch still get their chance to merge",
            "Also added `category` field to the existing merge-conflict conflict dict ('merge_conflict') for taxonomy consistency; `strategy_applied` is preserved so existing tests stay green",
            "skills/development-harness/scripts/tests/test_scope_violation.py: new 276-line test module with 16 cases across 4 classes. TestIsWithinScope (6): literal match, recursive glob, non-recursive glob, non-match, empty scope rejects all, multi-glob any-match. TestScopeViolationsHelper (4): in-scope branch returns []; out-of-scope files returned verbatim; empty touches_paths flags every changed file; unknown branch yields []. TestReadWorktreeTouchesPaths (3): reads what dispatch seeded; missing sentinel returns None (skip check); empty list is preserved (not coerced to None). TestMergeBatchRejectsScopeViolator (3): scope violator rejected before merge with HEAD unchanged + branch preserved; well-scoped sibling merges alongside scope violator (scope failures don't trigger abort_batch); within-scope changes merge cleanly",
            "Validation evidence with per-layer timing: `python -m py_compile` on the edited module + new test file exits 0 (0.14s); `python -m unittest skills.development-harness.scripts.tests.test_scope_violation -v` passes 16/16 (4.0s); `python -m unittest discover skills/development-harness/scripts/tests` passes 160/160 up from 144 (27.8s) -- zero regressions in existing test_merge_batch.py despite merge_batch.py's internal changes"
          ]
        },
        {
          "id": "unit_023",
          "description": "Add .harness/.lock file-based mutex around merge_batch.py with O_EXCL; contention test verifies serialization",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/scripts/merge_batch.py: added _MergeLock context manager backed by os.open(path, O_CREAT | O_EXCL | O_WRONLY) on <root>/.harness/.lock. First acquirer wins immediately; subsequent acquirers block, polling on lock_poll_interval (default 0.1s) until the file is unlinked (normal release) or its mtime exceeds stale_after_seconds (take-over after crashed holder). If lock_timeout (default 300s) elapses, MergeError is raised. Lock file body is '<pid> <iso_ts>\\n' for human inspection and stale debugging. Release removes the file on both normal exit AND exception paths (via __exit__ always calling unlink, verified by test_release_removes_lock_file_even_on_exception). merge_batch() wraps its full flow in `with _MergeLock(root, timeout=..., stale_after_seconds=..., poll_interval=...)` -- factored the original body into _merge_batch_locked(). CLI gets new --lock-timeout and --lock-stale-after flags passing through to merge_batch. Defaults: DEFAULT_LOCK_TIMEOUT=300.0, DEFAULT_LOCK_STALE_AFTER=600.0, DEFAULT_LOCK_POLL_INTERVAL=0.1",
            "skills/development-harness/scripts/tests/test_merge_batch.py: added TestLockContention with 4 cases. test_second_acquirer_blocks_until_first_releases: thread A acquires + holds for 0.4s; thread B blocks then acquires; assertion `b_time >= 0.3` verifies B waited. Event-barrier ensures A owns the lock before B tries, making the test robust against scheduling jitter. test_release_removes_lock_file_even_on_exception: raising RuntimeError inside `with _MergeLock(...)` still unlinks the lock file. test_stale_lock_is_taken_over: writes a lock file with mtime 10000s old, acquires with stale_after=300 in <0.5s. test_timeout_raises_merge_error_when_lock_is_fresh: pre-existing FRESH lock, lock_timeout=0.3s, merge_batch raises MergeError after >=0.25s wall-clock with 'timed out' in the error text; the pre-existing lock file is preserved (not stomped)",
            "Backward compatibility: the lock wrapper is transparent to existing tests. test_merge_batch + test_scope_violation (32 cases) pass unchanged after the wrapper was introduced -- the fresh tempdir per test means no cross-test lock contention",
            "Validation evidence with per-layer timing: `python -m py_compile skills/development-harness/scripts/merge_batch.py` exits 0 (0.14s); `python -m unittest skills.development-harness.scripts.tests.test_merge_batch.TestLockContention` passes 4/4 (1.6s); `python -m unittest discover skills/development-harness/scripts/tests` passes 164/164 up from 160 (34.5s)"
          ]
        },
        {
          "id": "unit_024",
          "description": "Extend skills/development-harness/scripts/sync_harness.py to detect orphaned worktrees, stale fleet entries, and harness/batch_* branches lacking fleet entries",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/scripts/sync_harness.py: added _list_on_disk_worktrees (scans .harness/worktrees/<batch>/<unit>/ structure), _list_harness_branches (shells to `git branch --list --format=...`, filters for names matching `harness/batch_*/<unit>`, silently returns [] when git is unavailable -- sync is informational), and _detect_fleet_drift (cross-references state.execution.fleet.units against disk worktrees and git branches). run_sync now loads state.json (optional) and appends fleet-drift findings to the existing divergences array",
            "Three new divergence types reported with actionable ids: orphan_worktree {type, worktree_path, batch_id, unit_id} for on-disk dirs not in fleet; stale_fleet_entry {type, unit_id, worktree_path, branch, batch_id} for fleet entries whose worktree_path is missing on disk; orphan_branch {type, branch, batch_id, unit_id} for harness/batch_*/<unit> branches without fleet entries. Each carries enough identity for downstream cleanup (feed batch_id to teardown_batch, unit_id to re-dispatch, etc.)",
            "skills/development-harness/scripts/tests/test_sync_harness.py: added TestFleetDriftDetection (5 cases) with helpers _init_git_repo, _write_state_with_fleet, _minimal_phase_graph. Cases: on-disk worktree without fleet entry is orphan; fleet entry without on-disk worktree is stale; harness branch without fleet entry is orphan; clean state (worktree + fleet entry + matching branch) yields zero fleet-drift divergences; one of each divergence type in the same run is reported separately (by_type dict). Existing 3 TestSyncHarness tests pass unchanged",
            "Validation evidence with per-layer timing: `python -m py_compile skills/development-harness/scripts/sync_harness.py` exits 0 (~0.1s); `python -m unittest skills.development-harness.scripts.tests.test_sync_harness -v` passes 8/8 (2.1s); `python -m unittest discover skills/development-harness/scripts/tests` passes 169/169 up from 164 (35.3s)"
          ]
        },
        {
          "id": "unit_025",
          "description": "Integration test skills/development-harness/scripts/tests/integration/test_parallel_invoke.py: fixture git repo with 3-unit phase, shell-scripted fake agents, asserts final state and cleanup",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/scripts/tests/integration/__init__.py (new empty file) + skills/development-harness/scripts/tests/integration/test_parallel_invoke.py (new 230-line integration module) under a fresh integration/ package. Each test uses its own tempdir git repo so tests are independent",
            "TestParallelInvokeHappyPath.test_three_unit_dispatch_merge_cycle: 3-unit batch over disjoint file trees (src/a/**, src/b/**, src/c/**); dispatch_batch creates worktrees + branches + fleet; shell-scripted fake agents commit canned files inside each worktree (mirrors what a real sub-agent would do); merge_batch fans the three branches back into main. Asserts outcome='ok'; every unit status='merged' with conflict=null + ended_at stamped; fleet.mode='idle'; every unit's files are present on main via `git cat-file -e HEAD:<path>`; `harness: merge <unit_id>` subjects are present in `git log`; NO residual .harness/worktrees/<batch_id>/ dir; NO residual harness/<batch_id>/* branches",
            "TestParallelInvokeWithScopeViolator.test_scope_violator_rejected_other_units_merge: mixed batch -- two well-scoped units (src/x/**, src/z/**) + one scope violator that declared src/y/** but committed to src/elsewhere/secrets.ts. merge_batch returns outcome='partial' with merged=['unit_good1','unit_good2'] and conflicted=['unit_bad']. unit_bad carries conflict.category='scope_violation' and conflict.paths includes src/elsewhere/secrets.ts. Asserts good units' files landed on main; violator's out-of-scope AND in-scope files did NOT (whole unit rejected); merged worktrees/branches are gone; violator's worktree + branch are preserved for operator inspection",
            "skills/development-harness/scripts/merge_batch.py: added _prune_empty_dir helper (no-op on missing / non-empty dirs, OS-error safe); merge_batch now prunes `.harness/worktrees/<batch_id>/` and `.harness/worktrees/` on the success path after per-unit cleanup. This matches the teardown_batch pattern and keeps sync_harness output clean. A serialize_conflicted unit or a scope-violator keeps its worktree, so the batch dir only disappears when the whole batch is resolved -- verified by the scope-violator integration test which sees the batch dir persist because unit_bad's worktree is still there",
            "Validation evidence with per-layer timing: `python -m py_compile skills/development-harness/scripts/merge_batch.py skills/development-harness/scripts/tests/integration/test_parallel_invoke.py` exits 0 (~0.1s); `python -m unittest skills.development-harness.scripts.tests.integration.test_parallel_invoke -v` passes 2/2 (2.9s); `python -m unittest discover skills/development-harness/scripts/tests` passes 171/171 up from 169 (36.1s)"
          ]
        }
      ]
    },
    {
      "id": "PHASE_006",
      "slug": "orchestrator-agent-contract",
      "status": "completed",
      "depends_on": [],
      "started": "2026-04-20T03:15:00Z",
      "completed": "2026-04-20T03:50:00Z",
      "units": [
        {
          "id": "unit_026",
          "description": "New skills/development-harness/templates/claude-code/agents/harness-unit.md: system prompt, tool allowlist (no git push; no writes to .harness/; no writes outside worktree), required JSON report schema",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/templates/claude-code/agents/harness-unit.md: new ~170-line agent definition file under a new templates/claude-code/agents/ directory. YAML frontmatter carries name='harness-unit', a one-paragraph description of the agent's role (execute one unit inside its dispatched worktree, commit on the pre-created branch, emit structured JSON report), and tools=[Read, Edit, Write, Glob, Grep, Bash]",
            "System-prompt body has 5 sections: Identity (read .harness/WORKTREE_UNIT.json first -- carries batch_id/unit_id/phase_id/touches_paths; stop immediately if the file is missing); Tool allowlist hard rules (Read/Edit/Write worktree-only, no .harness/ edits, Glob/Grep read-only, Bash restricted to git status/diff/log/add/commit + project test/lint commands, forbidden list covers git push/merge/rebase/reset --hard/checkout -b/worktree add/remove and anything writing outside the worktree or touching .harness/); Workflow (read identity -> read phase doc -> optional explore -> implement -> validate -> commit on pre-created branch -> emit JSON report); Report schema (unit_id, status, validation_evidence, commits, touched_paths_actual, failure) with per-field semantics; Reporting examples (success + validation failure + scope_violation failure); Hard rules summary",
            "JSON report schema is baked in verbatim: unit_id (verbatim from WORKTREE_UNIT.json), status='succeeded'|'failed', validation_evidence as list of timed evidence strings, commits as list of full SHAs, touched_paths_actual as subset of the declared touches_paths globs, failure=null or {category: 'validation'|'scope_violation'|'ambiguity'|'infrastructure', detail: <prose>}",
            "Acceptance criteria met: file exists; frontmatter + prompt + allowlist + report schema all present; prompt references .harness/WORKTREE_UNIT.json multiple times (grep finds it at lines 21, 23, 25, 40, 48, 49, 59, 90, 166)",
            "python -m unittest discover skills/development-harness/scripts/tests exits 0 (171/171) -- docs-only unit, test suite unchanged"
          ]
        },
        {
          "id": "unit_027",
          "description": "Update skills/development-harness/templates/claude-code/rules/harness-core.md with orchestrator-vs-sub-agent rules (only orchestrator merges; sub-agents commit only in worktree; never push/rebase)",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/templates/claude-code/rules/harness-core.md: appended new '## Orchestrator vs sub-agent boundary' section below the existing Delegation section. Three bullets verbatim in the phase-doc order: (1) 'Only the orchestrator modifies .harness/ or runs git merge' -- orchestrator definition, sub-agents treat .harness/ as read-only, fan-in gated by scope-violation check + .harness/.lock mutex; (2) 'Sub-agents commit only within their worktree branch; never git push, never git rebase, never git reset --hard' -- pre-created harness/<batch_id>/<unit_id> branch, orchestrator owns any history rewrite; (3) 'A worktree containing .harness/WORKTREE_UNIT.json is a fan-out environment' -- sentinel detection tells the agent which contract to follow (harness-unit) and explicitly notes the sentinel-absent case is orchestrator / ordinary session",
            "Grep verification on acceptance criteria: 'Only the orchestrator' at line 34, 'Sub-agents commit only' at line 35, 'fan-out environment' at line 36 (all three bullets present under the new section header)",
            "python -m unittest discover skills/development-harness/scripts/tests exits 0 (171/171) -- docs-only change, test suite unchanged"
          ]
        },
        {
          "id": "unit_028",
          "description": "Mirror orchestrator-vs-sub-agent rules in skills/development-harness/templates/rules/harness-core.mdc for Cursor",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/templates/rules/harness-core.mdc: appended '## Orchestrator vs sub-agent boundary' mirroring the Claude Code version with Cursor-specific wording. A short preamble notes that Cursor has no native agent-dispatch primitive, so the rules describe a contract for a human operator or second Cursor session rather than a framework-enforced allowlist. Three bullets match the phase-doc order: (1) only the orchestrator modifies .harness/ or runs git merge -- orchestrator defined as the Cursor session owning live .harness/ state; (2) sub-agent sessions commit only within their worktree branch, never push/rebase/reset --hard -- pre-named harness/<batch_id>/<unit_id>; (3) WORKTREE_UNIT.json is the fan-out sentinel, and the JSON report format from the Claude Code harness-unit template is tool-agnostic",
            "Cross-doc consistency verified via diff: the only deltas between templates/claude-code/rules/harness-core.md and templates/rules/harness-core.mdc are (a) the pre-existing frontmatter delimiter (paths: vs globs:), (b) the pre-existing rules-directory path (.claude/rules/ vs .cursor/rules/), and (c) the three new boundary bullets whose wording differs as required: Claude Code cites `Agent(subagent_type: \"harness-unit\")` and its own agent template; Cursor uses the 'second Cursor session' framing with a preamble explaining the no-native-Agent constraint",
            "Grep verification on acceptance criteria: 'Only the orchestrator' at line 35, 'Sub-agent sessions commit only' at line 36, 'fan-out environment' at line 37 -- all three bullets present with Cursor-appropriate wording",
            "python -m unittest discover skills/development-harness/scripts/tests exits 0 (171/171) -- docs-only change, test suite unchanged"
          ]
        }
      ]
    },
    {
      "id": "PHASE_007",
      "slug": "invoke-command-rewrite",
      "status": "completed",
      "depends_on": ["PHASE_005", "PHASE_006"],
      "started": "2026-04-20T03:55:00Z",
      "completed": "2026-04-20T04:50:00Z",
      "units": [
        {
          "id": "unit_029",
          "description": "Rewrite skills/development-harness/commands/invoke.md into a single batch-driven flow: batch of 1 runs in-tree, batch of N fans out via Agent(harness-unit); same dispatch/merge/validate/commit checkpoints",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/commands/invoke.md: whole-file rewrite from 432 -> 332 lines. New 12-step batch-driven structure (Steps 0-11): 0 Detect Host Tool, 1 Activate Invoke Session, 2 Validate, 3 Load State (with new fleet.mode!=idle guard), 4 Compute Batch (select_next_unit --frontier piped into compute_parallel_batch), 5 Dispatch (in-tree fast path for size==1 && parallelism.enabled==false, else dispatch_batch.py), 6 Execute (inline for in-tree with optional Explore + multi-file parallel-edits + parallel Layer 1+Layer 2 validation; Agent(harness-unit) fan-out per unit in one assistant message for worktree mode), 7 Verify (scope-violation check + report hygiene), 8 Merge (no-op in-tree; merge_batch.py with .harness/.lock + scope check + post-merge validation + worktree cleanup for fan-out), 9 State Update & Phase Completion (9a phase-graph, 9b state.json with session_count +=1 per turn, 9c conditional phase completion review, 9d checkpoint.md), 10 Commit (with parallel code-review + commit-agent-changes dispatch at phase close), 11 Turn Ends (with ISSUE_002 note pointing Claude Code users at /loop /invoke-development-harness for multi-turn runs)",
            "Acceptance criterion met: zero 'sequential path' / 'parallel path' fork language remains -- grep returns no matches. Every step applies to both batch sizes; only Steps 5 and 6 branch on the batch-size decision, and the branching is surfaced as 'In-tree fast path' vs 'Worktree fan-out' sub-sections within the single-step numbering",
            "fleet.mode transitions documented end-to-end: Step 3 guards against non-idle start state; Step 5 in-tree keeps 'idle' / Step 5 fan-out flips to 'dispatched'; Step 8 fan-out flips to 'merging' during merge_batch and back to 'idle' on completion; Step 11 Stop Conditions surfaces 'fleet.mode != idle at start of turn' as a recovery trigger",
            "One-turn-per-batch semantics documented in Step 9b: 'session_count -> increment by 1 (one turn, regardless of batch size)'. unit_031 will add the architecture.md paragraph",
            "Parallel phase-review dispatch (code-review concurrent with commit-agent-changes) lives in Step 10 with the concrete two-Agent-call template and the serial fallback for when only one skill is installed -- satisfies unit_032's acceptance in advance",
            "ISSUE_002 workaround documented in Step 11: Claude Code's one-shot stop_hook_active is called out with /loop /invoke-development-harness as the recommended multi-turn driver; Cursor's hook behavior is preserved",
            "Preserved content from prior PHASE docs: Step 6 in-tree carries unit_011's Exploration keywords, unit_013's multi-file parallel-edit thresholds, and unit_016's parallel-validation-layers (config.execution_mode.agent_delegation.parallel_validation_layers == true). Step 10 carries unit_014's parallel-dispatch rule. No capability regressions from previous phases",
            "python -m unittest discover skills/development-harness/scripts/tests exits 0 (171/171) -- docs-only change, test suite unchanged"
          ]
        },
        {
          "id": "unit_030",
          "description": "Mirror the single-flow rewrite in skills/development-harness/templates/workspace-commands/invoke-development-harness.md",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/templates/workspace-commands/invoke-development-harness.md: whole-file rewrite to mirror the unit_029 command-doc batch-driven structure in the compact-template style. 12 sections (0-11), one paragraph per section where possible. Sections 0-3 cover Detect Host Tool / Activate Invoke Session (with stop-hook no-op protection) / Detect Python / Validate (with fleet.mode!=idle guard). Section 4 pipes select_next_unit --frontier into compute_parallel_batch.py and picks dispatch mode. Sections 5 and 6 are the only top-level branches (in-tree vs worktree fan-out); sections 7-11 converge",
            "Section 5 Dispatch: in-tree keeps fleet.mode='idle'; worktree fan-out runs dispatch_batch.py and flips fleet.mode to 'dispatched'",
            "Section 6 Execute: in-tree carries the Exploration / multi-file parallel-edits / parallel Layer 1+Layer 2 validation guidance from units 011/013/016, condensed. Worktree fan-out dispatches one Agent(subagent_type='harness-unit') per unit in ONE assistant message and parses the returned JSON reports",
            "Section 7 Verify: in-tree does git diff scope check against touches_paths; worktree fan-out defers the scope check to merge_batch.py (unit_022) but still runs report-hygiene checks on sub-agent reports. Hygiene violations -> category='infrastructure'",
            "Section 8 Merge: no-op in-tree; merge_batch.py for fan-out with full flow (.harness/.lock -> merging -> scope+merge+validate -> idle)",
            "Section 9 State Update & Phase Completion: 9a phase-graph (completed vs pending-with-blocker); 9b state.json with 'session_count += 1 per turn regardless of batch size' made explicit; 9c Phase Completion Review (checklist + deploy gate); 9d checkpoint.md refresh",
            "Section 10 Commit: parallel code-review + commit-agent-changes dispatch at phase close in ONE assistant message; serial fallback when only one skill is installed",
            "Section 11 Turn Ends: standard stop-hook authority chain + ISSUE_002 note pointing Claude Code users at /loop /invoke-development-harness for multi-turn runs; Cursor hook behavior preserved",
            "Acceptance criteria met: same 12-step numbering (0 through 11) as command doc; same in-tree vs worktree fan-out branching in Steps 5/6; same fleet.mode transitions; same single-assistant-message Agent(harness-unit) dispatch pattern; same ISSUE_002 /loop workaround note in Step 11; zero 'sequential path' / 'parallel path' fork language (grep returns no matches)",
            "Grep verification: 12 monotonic section headings (lines 11, 20, 28, 35, 43, 61, 71, 77, 83, 93, 103, 115); fleet.mode appears at sections 3/5/8 with correct transition semantics",
            "python -m unittest discover skills/development-harness/scripts/tests exits 0 (171/171) -- docs-only change, test suite unchanged"
          ]
        },
        {
          "id": "unit_031",
          "description": "Document one-turn-per-batch semantics in skills/development-harness/references/architecture.md (session_count increments once per turn)",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/references/architecture.md: new '## Batch Semantics — one turn per batch' subsection inserted after Loop Mechanics and before Git Integration. Makes three load-bearing statements: (1) a single invoke turn processes a whole BATCH not a single unit; (2) execution.session_count increments by exactly 1 per turn REGARDLESS of batch size (so loop_budget is denominated in turns, not units); (3) the full fleet-mode transition idle -> dispatched -> merging -> idle completes within one turn in the worktree path, so the stop hook never sees a partial fleet. Links to commands/invoke.md Steps 4-9 and to parallel-execution.md (PHASE_011 unit_047 will create that doc; this reference is a forward-link placeholder)",
            "Acceptance criteria met: new paragraph explicitly states session_count increments once per turn; explicitly links to PHASE_007 rewrite rationale via the commands/invoke.md Steps 4-9 reference",
            "Grep verification: '## Batch Semantics — one turn per batch' matches at line 81; 'session_count increments by exactly 1 per turn, regardless of batch size' present at line 85",
            "python -m unittest discover skills/development-harness/scripts/tests exits 0 (171/171) -- docs-only change"
          ]
        },
        {
          "id": "unit_032",
          "description": "Wire parallel phase-review dispatch (code-review skill concurrent with commit-agent-changes) into the rewritten flow",
          "status": "completed",
          "validation_evidence": [
            "Pre-satisfied by unit_029 and unit_030. The batch-driven invoke rewrite landed the parallel phase-review dispatch rule directly in Step 10 (Commit) of both commands/invoke.md and the workspace-commands template. At phase close with both code-review and commit-agent-changes installed, the flow dispatches them in ONE assistant message with two Agent(subagent_type='general-purpose') tool calls -- one runs code-review read-only on the branch diff, the other runs commit-agent-changes to commit/push/open-PR. Serial fallback when only one skill is installed or no phase closed this turn",
            "Grep verification (acceptance: 'single assistant message with both Agent calls'): commands/invoke.md line 262 carries 'in one assistant message with two Agent(subagent_type: \"general-purpose\") tool calls'; templates/workspace-commands/invoke-development-harness.md line 111 carries the same phrasing compressed to the template style. Both include the two-call code sample and the serial fallback",
            "python -m unittest discover skills/development-harness/scripts/tests exits 0 (171/171) -- no additional file changes this unit; acceptance was landed in earlier commits this phase"
          ]
        },
        {
          "id": "unit_033",
          "description": "End-to-end integration test: 3-unit fixture phase with parallelism.enabled=true completes with one merge; batch-of-1 test produces identical final state to a worktree batch of 1",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/scripts/tests/integration/test_invoke_rewrite.py: new 288-line integration module with 2 test classes. TestThreeUnitParallelBatch.test_one_turn_one_batch_end_to_end: seeds a 3-unit phase-graph with disjoint touches_paths, runs compute_frontier -> compute_batch(parallelism=enabled) -> dispatch_batch -> 3 fake agents committing inside their worktrees -> merge_batch in one pass. Asserts batch packs all three units; merge_result.outcome=='ok' and merged=['unit_a','unit_b','unit_c']; every unit ends with phase-graph status='completed' + non-empty validation_evidence; state.execution.fleet.mode=='idle' at end of turn; all touches_paths files present on main; no residual worktree dirs and no residual harness/<batch>/* branches",
            "TestBatchOfOneDispatchModeEquivalence.test_batch_of_one_in_tree_matches_worktree_logical_state: runs the same single unit (touches_paths=['src/x/**']) through both dispatch modes on fresh fixture repos -- in-tree (compute_batch with parallelism=disabled, edit + commit on main directly, verify scope via git diff, no-op merge) vs worktree fan-out (compute_batch with parallelism=enabled, dispatch_batch, fake agent in worktree, merge_batch). Compares logical state (unit status + evidence count + files-on-main) via a helper; asserts strict equality regardless of SHA differences. Both paths end with fleet.mode=='idle'",
            "Test operates at a higher abstraction than the PHASE_005 test_parallel_invoke.py integration -- starts from a realistic phase-graph.json-shaped dict and exercises compute_frontier + compute_parallel_batch.compute_batch, catching regressions in the full pipeline that a hand-crafted fleet state would miss",
            "Validation evidence with per-layer timing: `python -m py_compile skills/development-harness/scripts/tests/integration/test_invoke_rewrite.py` exits 0 (~0.1s); `python -m unittest skills.development-harness.scripts.tests.integration.test_invoke_rewrite -v` passes 2/2 (3.5s); `python -m unittest discover skills/development-harness/scripts/tests` passes 173/173 up from 171 (45.0s)"
          ]
        }
      ]
    },
    {
      "id": "PHASE_008",
      "slug": "stop-hook-fleet-awareness",
      "status": "completed",
      "depends_on": ["PHASE_001", "PHASE_007"],
      "started": "2026-04-20T04:55:00Z",
      "completed": "2026-04-20T05:20:00Z",
      "units": [
        {
          "id": "unit_034",
          "description": "Update skills/development-harness/templates/claude-code/hooks/continue-loop.py: pre-check that stops when state.execution.fleet.mode != 'idle'; deletes .invoke-active on stop",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/templates/claude-code/hooks/continue-loop.py: added a fleet-mode pre-check between 'read state.json' and 'check loop budget/blockers/open_questions'. Reads execution.fleet.mode with a safe chain (execution.get('fleet') or {}).get('mode', 'idle'), so a missing fleet block (v1-style state) is treated as idle. If mode is not 'idle' (i.e., 'dispatched' or 'merging'), the hook calls _stop(cwd) which deletes .invoke-active and exits 0. Module docstring updated to reflect the new authority-chain step",
            "skills/development-harness/scripts/tests/test_continue_loop_claude.py: new 200-line test module with 5 cases across 2 classes. TestFleetModeGuard: fleet.mode='dispatched' stops and removes .invoke-active; fleet.mode='merging' stops and removes flag; fleet.mode='idle' passes the guard (falls through to downstream stop); missing fleet block treated as idle. TestFleetGuardDoesNotMaskContinuePath: positive control -- with fleet.mode='idle' AND a fully-seeded harness (phase-graph, select_next_unit stub, checkpoint.next_action matching the unit id), the hook reaches exit 2 with 'block' decision, proving the guard does not falsely short-circuit on idle state",
            "Test harness technique: runs the hook as a subprocess with controlled stdin payload; asserts exit code + .invoke-active file presence/absence as the observable contract. Does NOT depend on the real select_next_unit.py -- the positive-control test ships a tiny one-off selector stub to isolate the hook from selector evolution",
            "Validation evidence with per-layer timing: `python -m py_compile` on hook + test exits 0 (~0.1s); `python -m unittest skills.development-harness.scripts.tests.test_continue_loop_claude -v` passes 5/5 (0.5s); `python -m unittest discover skills/development-harness/scripts/tests` passes 178/178 up from 173 (39.6s)"
          ]
        },
        {
          "id": "unit_035",
          "description": "Mirror fleet-mode guard in skills/development-harness/templates/hooks/continue-loop.py (Cursor variant)",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/templates/hooks/continue-loop.py: mirrored the fleet-mode guard from unit_034's Claude Code hook. Same safe chain (execution.get('fleet') or {}).get('mode', 'idle') so missing fleet block (v1 state) is treated as idle; non-idle mode calls _stop(invoke_flag) which removes .invoke-active and prints `{}` (Cursor's stop signal). Sits between state.json read and the existing loop_budget / blockers / open_questions chain. Authority-chain docstring updated",
            "Minor code cleanup: factored 'execution = state.get(\"execution\", {})' out of the loop_budget line so the fleet-mode guard and loop_budget share the same reference (same pattern as the Claude Code variant). No behavior change",
            "skills/development-harness/scripts/tests/test_continue_loop_cursor.py: new 180-line test module with 5 cases across 2 classes, structurally mirroring test_continue_loop_claude.py but adapted for Cursor's always-exit-0 + JSON-payload stop protocol. Tests inspect stdout JSON (`{}` = stop, `{followup_message: ...}` = continue) rather than returncode. TestFleetModeGuard: dispatched stops + removes flag; merging stops + removes flag; idle falls through; missing fleet block treated as idle. TestFleetGuardDoesNotMaskContinuePath: positive control with fully-seeded harness + stub selector reaches the followup_message path and preserves .invoke-active, proving the guard doesn't falsely short-circuit idle state",
            "Validation evidence with per-layer timing: `python -m py_compile` on both files exits 0 (~0.1s); `python -m unittest skills.development-harness.scripts.tests.test_continue_loop_cursor -v` passes 5/5 (0.6s); `python -m unittest discover skills/development-harness/scripts/tests` passes 183/183 up from 178 (44.5s)"
          ]
        },
        {
          "id": "unit_036",
          "description": "Unit tests for both hooks simulating fleet.mode = 'dispatched' (stop) and 'idle' (existing behavior); manual verification checklist for mid-batch kill",
          "status": "completed",
          "validation_evidence": [
            "First half of the acceptance (unit tests for both hooks simulating dispatched + idle) is covered by unit_034's test_continue_loop_claude.py and unit_035's test_continue_loop_cursor.py. Ten cases total across both files cover dispatched, merging, idle-falls-through, missing-fleet-treated-as-idle, and a positive-control continue path per hook variant",
            "Second half (manual verification checklist for mid-batch kill) landed as a new subsection in PHASES/PHASE_008_stop-hook-fleet-awareness.md under 'Rollback / Failure Considerations'. The checklist walks through: set up a live invoke session with parallelism enabled; kill the turn mid-batch after fleet.mode flips to 'dispatched'; start a new session and confirm the hook stops cleanly (.invoke-active removed); run /sync-development-harness and confirm orphan_worktree + orphan_branch divergences surface; run teardown_batch.py --batch-id <id> to clean up; confirm /invoke-development-harness restarts cleanly afterward",
            "No additional code changes for this unit -- the acceptance was 'Documented checklist captured in this phase's rollback section', which is now satisfied by the phase-doc edit",
            "python -m unittest discover skills/development-harness/scripts/tests exits 0 (183/183) -- no code changes in this unit"
          ]
        }
      ]
    },
    {
      "id": "PHASE_009",
      "slug": "safety-rails-and-automatic-fallback",
      "status": "completed",
      "depends_on": ["PHASE_007"],
      "started": "2026-04-20T05:30:00Z",
      "completed": "2026-04-20T07:30:00Z",
      "units": [
        {
          "id": "unit_037",
          "description": "Session-scoped kill switch: write .harness/.parallel-disabled after 2 scope_violation or ambiguity failures in a session; cleared when .invoke-active clears",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/scripts/safety_rails.py: new 165-line stdlib-only module with record_failure(root, category, unit_id, now) (appends a JSON line to .harness/.parallel-failures.jsonl and writes .harness/.parallel-disabled when counted-category failures reach KILL_SWITCH_THRESHOLD=2), is_parallel_disabled(root) (presence check), clear_safety_rails(root) (idempotent removal of both files). COUNTED_CATEGORIES = ('scope_violation', 'ambiguity'). Failures in other categories are still logged for observability but don't count toward the kill switch. CLI: record / status / clear subcommands with --root --category --unit-id",
            "skills/development-harness/templates/claude-code/hooks/continue-loop.py + templates/hooks/continue-loop.py: both hooks' _stop() helpers now remove .invoke-active AND .parallel-disabled AND .parallel-failures.jsonl in one sweep. Session-scoped safety-rail state is cleared whenever the hook decides to stop, so the next invoke session starts clean. Cursor's _stop derives harness_dir from the invoke_flag path (os.path.dirname(invoke_flag)); Claude Code's uses cwd directly",
            "skills/development-harness/scripts/tests/test_safety_rails.py: new 225-line test module with 15 cases across 5 classes. TestRecordFailureThreshold (6): single failure doesn't trip; two failures trip; mixed scope_violation+ambiguity trip together; third call is idempotent; non-counted categories never count; counted + non-counted mix counts only the counted ones. TestHelpers (3): is_parallel_disabled file-presence reflection; clear removes both files; clear is idempotent on missing files. TestCliSmoke (2): record-then-status round trip; CLI clear. TestHookStopClearsSafetyRails (2): Claude Code AND Cursor hooks' _stop() wipe .parallel-disabled + .parallel-failures.jsonl along with .invoke-active. TestConstants (2): threshold=2; COUNTED_CATEGORIES set",
            "Validation evidence with per-layer timing: `python -m py_compile` on all four changed/new files exits 0 (~0.1s); `python -m unittest skills.development-harness.scripts.tests.test_safety_rails -v` passes 15/15 (0.7s); `python -m unittest discover skills/development-harness/scripts/tests` passes 198/198 up from 183 (38.4s). Pre-existing test_continue_loop_claude + test_continue_loop_cursor (10 cases) pass unchanged despite the _stop() changes -- the additional file removals target missing files in those tests, which the unlink try/except handles cleanly"
          ]
        },
        {
          "id": "unit_038",
          "description": "Verify batch-of-1 uses the same in-tree path as parallelism=false; re-assert via test that no special-case branch exists in invoke command docs",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/commands/invoke.md step 4 'Pick the dispatch mode' gates the in-tree fast path on `len(batch) == 1 AND config.execution_mode.parallelism.enabled == false`; the companion 'Worktree fan-out' bullet explicitly covers `len(batch) == 1` when parallelism is on, so batch-of-1 is NOT a sequential-path special case. skills/development-harness/templates/workspace-commands/invoke-development-harness.md step 4 mirrors the same two-clause gating. No 'batch-of-1 -> sequential' fork remains anywhere in the invoke docs post-PHASE_007 rewrite",
            "skills/development-harness/scripts/tests/integration/test_invoke_rewrite.py TestBatchOfOneDispatchModeEquivalence (landed in PHASE_007 unit_033) already asserts logical-state equivalence between a single unit run through the in-tree fast path and a single unit run through the worktree fan-out (unit status, evidence count, touches_paths file contents on main). It also pins the substantive invariant that the in-tree path leaves state.execution.fleet byte-identical via a copy.deepcopy before/after compare -- the integration half of unit_038's acceptance is pre-satisfied",
            "skills/development-harness/scripts/tests/test_safety_rails.py: new class TestInvokeDocHasNoBatchOfOneSpecialCase (3 cases) promotes the grep-level check into a regression test so a future doc edit can't silently reintroduce the fork. test_in_tree_gating_always_includes_parallelism_clause asserts every in-tree-gating line in both docs names the parallelism=false clause. test_no_batch_of_one_sequential_antipattern rejects the pre-PHASE_007 'batch-of-1 -> sequential' phrasings. test_batch_of_one_with_parallelism_on_goes_to_worktree pins the sentence that routes batch-of-1 through worktree fan-out when parallelism is on",
            "Validation evidence with per-layer timing: `python -m py_compile skills/development-harness/scripts/tests/test_safety_rails.py` exits 0 (~0.1s); `python -m unittest skills.development-harness.scripts.tests.test_safety_rails -v` passes 18/18 (0.77s, up from 15 -- 3 new cases for unit_038); `python -m unittest discover skills/development-harness/scripts/tests` passes 201/201 (46.6s, up from 198)"
          ]
        },
        {
          "id": "unit_039",
          "description": "Document scope-violation always-on policy in skills/development-harness/references/phase-contract.md",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/references/phase-contract.md: added new 'Scope-Violation Enforcement Policy' top-level section (after 'Contract Enforcement') that states scope-violation detection is always on regardless of configuration, explains that `config.execution_mode.parallelism.require_touches_paths` is an admission-control knob on compute_parallel_batch (not a merge-time enforcement toggle), and gives the trust-boundary rationale: touches_paths is a trust boundary declaration, the sub-agent is untrusted (worktree-confined tool allowlist, no push/merge/rebase), and the git diff against declared globs is the orchestrator's only guarantee that the sub-agent didn't step outside its lane. Includes three sub-subsections: 'What require_touches_paths does NOT do', 'Rationale: trust boundary, not feature toggle', and 'Downstream consequences' (scope violation is a hard reject regardless of conflict_strategy; counts toward the session kill switch; is a per-phase contract invariant that no phase doc may opt out of)",
            "Policy grounding verified in code: skills/development-harness/scripts/merge_batch.py L400-411 runs the scope check unconditionally when WORKTREE_UNIT.json carries a non-null touches_paths list -- require_touches_paths is never read there. skills/development-harness/scripts/compute_parallel_batch.py L148 is the only site that reads require_touches_paths; it gates batch admission for units that lack a touches_paths declaration. Both file+line references match the doc's claims",
            "skills/development-harness/scripts/tests/test_safety_rails.py: new TestScopeViolationAlwaysOnPolicy class (3 cases). test_policy_section_present: 'Scope-Violation Enforcement Policy' heading is present AND 'always on, regardless of configuration' statement is present (normalized for markdown bold). test_policy_explains_require_touches_paths_is_admission_control: the doc classifies the knob as 'admission-control', names compute_parallel_batch as the site, and contrasts admission-time vs merge-time enforcement. test_policy_gives_trust_boundary_rationale: the doc frames touches_paths as a 'trust-boundary', names the 'sub-agent' as the untrusted producer, and names 'diff' as the source of truth",
            "Validation evidence with per-layer timing: python -m py_compile skills/development-harness/scripts/tests/test_safety_rails.py exits 0 (~0.1s); python -m unittest skills.development-harness.scripts.tests.test_safety_rails.TestScopeViolationAlwaysOnPolicy -v passes 3/3 (0.001s); python -m unittest discover skills/development-harness/scripts/tests passes 204/204 (41.0s, up from 201 -- 3 new cases for unit_039)"
          ]
        },
        {
          "id": "unit_040",
          "description": "Concurrent merge serialization test: two merge_batch.py invocations contend on .harness/.lock; second waits for first",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/scripts/tests/test_merge_batch.py TestLockContention: acceptance largely pre-satisfied by PHASE_005 unit_023's four existing cases (test_second_acquirer_blocks_until_first_releases via two threads, test_release_removes_lock_file_even_on_exception, test_stale_lock_is_taken_over, test_timeout_raises_merge_error_when_lock_is_fresh). All four continue to pass",
            "unit_040 adds two new cases to close the 'two merge_batch.py invocations' wording in the acceptance -- the prior thread test proves the _MergeLock primitive; the new subprocess test proves OS-level O_EXCL semantics hold across processes (the production scenario when two CLI invocations race from two shells or schedulers). test_two_subprocess_acquirers_serialize_on_disk_lock: spawn a Python subprocess that acquires _MergeLock against the same .harness/.lock, signals 'HELD' on stdout, sleeps hold_seconds=0.5s, releases. The main process then acquires the same lock in-process and measures elapsed -- must be >= hold_seconds - 0.1s. After both release, the lock file is gone. PIPE handles explicitly closed to avoid Python 3.12 ResourceWarnings (verified via `python -W error::ResourceWarning`)",
            "test_lock_path_is_exactly_harness_dot_lock: pin the on-disk location. If anyone moves LOCK_FILENAME in merge_batch.py or changes the <root>/.harness/.lock layout, cross-process serialization would silently break (each invocation would acquire a different lock). This test trips before that can ship",
            "Validation evidence with per-layer timing: python -m py_compile skills/development-harness/scripts/tests/test_merge_batch.py exits 0 (~0.1s); python -m unittest skills.development-harness.scripts.tests.test_merge_batch.TestLockContention -v passes 6/6 (2.2s, up from 4 -- 2 new cases for unit_040); python -W error::ResourceWarning single-test run confirms clean handle management; python -m unittest discover skills/development-harness/scripts/tests passes 206/206 (34.7s, up from 204)"
          ]
        }
      ]
    },
    {
      "id": "PHASE_010",
      "slug": "observability",
      "status": "completed",
      "depends_on": ["PHASE_005", "PHASE_007"],
      "started": "2026-04-20T07:45:00Z",
      "completed": "2026-04-20T09:10:00Z",
      "units": [
        {
          "id": "unit_041",
          "description": "Extend skills/development-harness/templates/checkpoint-template.md with a Batch section (batch ID, mode, per-unit status and branch, conflicts summary)",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/templates/checkpoint-template.md: added new 'Batch (current or last)' second-level heading at the bottom of the template (above the trailing `--- Updated` line). Placeholders: {{BATCH_ID_OR_NONE}}, {{FLEET_MODE}}, {{BATCH_UNIT_ROWS_OR_NONE}}, {{BATCH_CONFLICTS_OR_NONE}}. Reflects state.execution.fleet. Guidance paragraph documents the idle-with-no-prior-batch render ('Batch ID: none', 'Mode: idle', empty unit table, 'No conflicts.')",
            "Per-unit markdown table column set: Unit, Phase, Status, Branch, Started, Ended. These are the fields already present on state.execution.fleet.units (from PHASE_005's dispatch/merge state shape), so the agent/script filling the template doesn't need to reshape the data",
            "skills/development-harness/scripts/tests/test_checkpoint_template.py: new 90-line test module with 1 class (TestCheckpointTemplateBatchSection) and 4 cases. test_batch_section_heading_present pins the heading. test_batch_section_placeholders_present pins all four new placeholders. test_batch_section_has_unit_table_columns extracts the table-header line and asserts each of the 6 column names is present, so a future edit that shuffles columns trips this test. test_pre_existing_placeholders_still_present pins the 7 pre-PHASE_010 placeholders to guard against accidental deletion during future edits",
            "Grounding: grep -r checkpoint-template.md skills/ confirms only commands/create.md references the template, and it does so narratively (no placeholder substitution in any runtime script). The template is seed material for a new project's initial checkpoint.md -- this edit cannot break runtime code because there is no runtime code that interprets the placeholders programmatically",
            "Validation evidence with per-layer timing: python -m py_compile skills/development-harness/scripts/tests/test_checkpoint_template.py exits 0 (~0.1s); python -m unittest skills.development-harness.scripts.tests.test_checkpoint_template -v passes 4/4 (0.001s); python -m unittest discover skills/development-harness/scripts/tests passes 210/210 (33.6s, up from 206 -- 4 new cases for unit_041)"
          ]
        },
        {
          "id": "unit_042",
          "description": "Update skills/development-harness/commands/state.md and templates/workspace-commands/harness-state.md to render fleet status, orphaned worktrees, and per-batch timings",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/commands/state.md: added 'Step 2b: Read Fleet & Batch State' (parses state.execution.fleet, runs sync_harness.py for orphan detection, documents the three divergence categories -- orphan_worktree, stale_fleet_entry, orphan_branch) and 'Step 2c: Read Per-Batch Timings' (sources from .harness/logs/<batch_id>/, specifies the exact one-line format 'Batch <batch_id>: dispatched HH:MM:SS, merged HH:MM:SS, total Ns', and forbids git-timestamp inference). Output Report section gained three new report sections -- 'Fleet & Batch' (mirrors checkpoint-template.md Batch section columns), 'Orphans' (never omitted -- renders 'No orphans detected.' when divergences empty), 'Batch Timings' (never omitted -- renders 'Batch timings: unavailable' when no logs yet). Two new 'Honesty Rules' explicitly pin the never-omit behavior for Orphans and Batch Timings. The section order is intentional: Fleet precedes Orphans precedes Timings, matching the causal chain dispatch -> divergence-check -> duration",
            "skills/development-harness/templates/workspace-commands/harness-state.md: mirror edit. Added step 5 (Read Fleet & Batch state + sync_harness.py) and step 6 (Read per-batch timings with the exact format + git-timestamp inference ban). Output template + Honesty Rules match the long-form doc byte-for-byte on the substantive claims (heading names, format strings, divergence categories). Ensures Cursor and Claude Code installs produce the same report shape",
            "skills/development-harness/scripts/tests/test_harness_state_docs.py: new 120-line test module with 1 class (TestHarnessStateDocCoverage) and 5 cases. test_both_docs_render_fleet_and_batch_section pins the '## Fleet & Batch' heading + fleet.mode/batch_id/units references. test_both_docs_render_orphans_section_via_sync_harness pins sync_harness.py mention + '## Orphans' heading + all three divergence categories + the 'No orphans detected.' empty-case render. test_both_docs_specify_exact_timing_line_format pins the '## Batch Timings' heading + the exact timing-line format string + the 'Batch timings: unavailable' fallback. test_both_docs_reference_dot_harness_logs_directory pins the .harness/logs/ directory layout reference. test_docs_forbid_git_timestamp_inference_for_timings pins the explicit 'do not infer timings from git timestamps' prohibition in both docs -- guards against an agent silently falling back to git timestamps when logs are missing",
            "Grounding: sync_harness.py is read-only (it is always a reporter, never a mutator; the phase doc's 'sync_harness.py --dry-run' phrasing is simplified into 'sync_harness.py' in both command docs to match actual script behavior). The three divergence categories in sync_harness.py's output are the exact set: orphan_worktree, stale_fleet_entry, orphan_branch (verified at skills/development-harness/scripts/sync_harness.py L211-255)",
            "Validation evidence with per-layer timing: python -m py_compile skills/development-harness/scripts/tests/test_harness_state_docs.py exits 0 (~0.1s); python -m unittest skills.development-harness.scripts.tests.test_harness_state_docs -v passes 5/5 (0.002s); python -m unittest discover skills/development-harness/scripts/tests passes 215/215 (33.5s, up from 210 -- 5 new cases for unit_042)"
          ]
        },
        {
          "id": "unit_043",
          "description": "Log directory creation logic for .harness/logs/<batch_id>/ with batch.json, <unit_id>.md, merge.log, validation.log; logs are harness-owned and gitignored",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/scripts/dispatch_batch.py: added LOGS_DIR = 'logs' module constant + new _write_batch_log(root, batch_id, filename, payload) helper. Helper creates .harness/logs/<batch_id>/ on demand via mkdir(parents=True, exist_ok=True), serializes dict/list payloads as indented JSON with a trailing newline, writes strings verbatim. Wraps everything in try/except OSError and returns False on failure -- never raises. After the fleet is built successfully, dispatch_batch writes batch.json with {batch_id, dispatched_at, batch_plan (the full batch_result input), fleet} so /harness-state has the plan + actual dispatched fleet side-by-side. The call site itself is also wrapped in try/except Exception as defense-in-depth: a future helper change that raises must not break dispatch",
            "skills/development-harness/scripts/merge_batch.py: mirror additions. LOGS_DIR constant, _write_batch_log helper (identical shape to the dispatch one, plus an empty/None batch_id short-circuit since the empty-fleet return path has no batch to log against), _render_merge_log(batch_id, result) that emits a grep-friendly multi-line summary (batch_id, outcome, merged/conflicted/skipped sections with one `- <unit_id>` bullet per entry). Two call sites write merge.log (the validation-failure early return + the happy-path end-of-function return), one call site writes validation.log (before the validation-failure rollback branch so a failed validation still leaves evidence on disk). All three call sites are wrapped in try/except Exception -- the orchestrator's critical path must not depend on log writes",
            "skills/development-harness/scripts/tests/test_batch_logs.py: new 290-line test module with 3 classes and 7 cases (1 OS-specific skip on Windows). TestDispatchWritesBatchJson.test_dispatch_produces_batch_json: 2-unit fixture batch, asserts batch.json exists, has {batch_id, dispatched_at, batch_plan, fleet} keys, fleet.mode='dispatched', and both unit_ids in fleet.units. TestMergeWritesMergeAndValidationLogs (3 cases): happy path (both logs present with expected bodies -- grep-friendly structure, validator's message verbatim); validation failure (both logs present + 'outcome: validation_failed' in merge.log + 'validator_ok: False' in validation.log); empty-fleet short-circuit (NO logs, because nothing ran to log -- absence is itself a reported fact). TestLogWritesAreBestEffort (3 cases): empty batch_id returns False (merge helper's guard); read-only dir returns False without raising (skipped on Windows with a clear skip message -- the empty-batch_id case and happy-path tests carry equivalent substance); dispatch_batch survives when the helper is monkeypatched to always raise OSError (proves the call site's own try/except Exception is load-bearing, not just the helper's internal swallow)",
            "skills/development-harness/.gitignore already carries logs/ from harness creation -- verified; no change needed (grep of .harness/.gitignore confirms). Log artifacts are harness-owned and gitignored per the phase doc",
            "Validation evidence with per-layer timing: python -m py_compile skills/development-harness/scripts/{dispatch_batch.py,merge_batch.py,tests/test_batch_logs.py} exits 0 (~0.1s); python -m unittest skills.development-harness.scripts.tests.test_batch_logs -v passes 6/7 with 1 OS-specific skip (2.9s); pre-existing tests skills.development-harness.scripts.tests.test_dispatch_batch + test_merge_batch still pass 31/31 (13.7s); python -m unittest discover skills/development-harness/scripts/tests passes 221/222 with 1 skip (40.2s, up from 215 -- 7 new cases for unit_043)"
          ]
        },
        {
          "id": "unit_044",
          "description": "Integration test running a sample batch asserts all log artifacts are produced and /harness-state renders fleet + log pointers",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/scripts/tests/integration/test_invoke_rewrite.py: added new TestBatchLogArtifactsEndToEnd class with one integration case (test_all_four_log_artifacts_present_after_batch). Also added missing 'import json' at the top of the module (caught by the first test run -- json was referenced via the new test but not previously imported). The test runs compute_parallel_batch -> dispatch_batch -> fake sub-agent commits in each worktree + sub-agent summary (.harness/logs/<batch_id>/<unit_id>.md, fabricated inline the same way the sub-agent would per templates/claude-code/agents/harness-unit.md) -> merge_batch with a non-trivial validator. Asserts all four log artifacts exist with non-empty bodies: batch.json (JSON-decodable + carries {batch_id, dispatched_at, batch_plan, fleet}), merge.log (grep-friendly: 'batch_id: <id>', 'outcome: ok', '- unit_log_a', '- unit_log_b'), validation.log ('validator_ok: True' + validator's exact message), and both <unit_id>.md summaries. Also asserts the worktrees/<batch_id>/ directory is pruned after a clean merge while logs/<batch_id>/ survives -- logs ARE the post-hoc inspection surface",
            "Grounding: the four artifacts exactly match the acceptance criteria from PHASE_010's phase doc scope. The <unit_id>.md produced by sub-agents per the harness-unit contract is covered by test-time fabrication because the sub-agent is not executable in a unit-test context; the fabrication writes what the sub-agent would write (unit_id heading + status + files-changed list) so the artifact shape is still pinned from the orchestrator's perspective",
            "Half of the unit's acceptance -- '/harness-state renders fleet + log pointers' -- was pre-satisfied by unit_042 (the /harness-state doc rewrite pins the Fleet & Batch, Orphans, and Batch Timings sections in both command variants, and unit_042's test_harness_state_docs.py already asserts the .harness/logs/ directory reference is present). The integration test's role here is to prove that a real end-to-end run produces the artifacts /harness-state will read; the doc-level contract was already locked in",
            "Validation evidence with per-layer timing: python -m py_compile skills/development-harness/scripts/tests/integration/test_invoke_rewrite.py exits 0 (~0.1s); python -m unittest skills.development-harness.scripts.tests.integration.test_invoke_rewrite.TestBatchLogArtifactsEndToEnd -v passes 1/1 (0.9s); pre-existing integration cases (TestThreeUnitParallelBatch + TestBatchOfOneDispatchModeEquivalence) still pass; python -m unittest discover skills/development-harness/scripts/tests passes 222/223 with 1 OS-specific skip (37.6s, up from 222 -- 1 new case for unit_044)"
          ]
        }
      ]
    },
    {
      "id": "PHASE_011",
      "slug": "documentation",
      "status": "completed",
      "depends_on": [],
      "started": "2026-04-20T09:25:00Z",
      "completed": "2026-04-20T12:00:00Z",
      "units": [
        {
          "id": "unit_bugfix_001",
          "description": "ISSUE_001 fix: in skills/development-harness/commands/create.md Phase 5 (Hook configuration), apply the existing Python-detection pattern (PY=$(command -v python3 2>/dev/null || command -v python 2>/dev/null)) and bake the detected interpreter into the generated .claude/settings.local.json Stop-hook command (\"$PY .claude/hooks/continue-loop.py\"). Mirror the change for .cursor/hooks.json. Keep the hook script's shebang intact but no longer rely on it.",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/commands/create.md Phase 5 'Hook configuration' subsection: rewrote the hook-generation prose so it (a) leads with the portable detection idiom (PY=$(command -v python3 2>/dev/null || command -v python 2>/dev/null) with the [ -z \"$PY\" ] exit-1 guard already used by all 7 workspace command templates + SKILL.md), (b) bakes the resolved interpreter into the generated command field for BOTH tool variants (\"$PY .claude/hooks/continue-loop.py\" for Claude Code, \"$PY .cursor/hooks/continue-loop.py\" for Cursor), and (c) explicitly instructs writing the resolved absolute path into the JSON ('Write the actual path into the JSON; do not leave the literal $PY in the file'). The hook script's shebang (#!/usr/bin/env python3) is preserved for direct-invocation ergonomics but is no longer on the hot path -- the shebang is never resolved at hook-execution time",
            "skills/development-harness/scripts/tests/test_hook_python_detection.py: new 120-line test module with 1 class (TestCreateDocHookPythonDetection) and 5 cases. test_hook_section_applies_python_detection pins the detection idiom's presence inside the Hook configuration subsection (the section is sliced out before matching so unrelated Python-detection snippets elsewhere in the doc cannot satisfy the assertion). test_hook_section_bakes_py_into_claude_code_command + test_hook_section_bakes_py_into_cursor_command pin the exact '$PY <path>' command-field strings for both variants. test_hook_section_forbids_bare_script_path_for_command is the explicit negative check that guards against reintroducing the pre-fix bare-path template ('\"command\": \".claude/hooks/continue-loop.py\"'). test_hook_section_instructs_resolving_py_placeholder pins the 'do not leave the literal $PY' guidance so readers don't misinterpret the JSON snippets as copy-paste-ready templates with an unresolved placeholder",
            ".harness/issues/ISSUE_001.json: status flipped from 'open' to 'resolved' with resolved timestamp 2026-04-20T09:30:00Z. The issue's regression_coverage note ('a unit test around a helper that produces the settings-hook JSON given a detected PY') is addressed at the doc-level since create.md is prose instructions the bootstrapping agent interprets -- there is no extractable helper to test, so a doc-shape contract with forbidden/required substring checks is the cleanest regression guard available",
            "Validation evidence with per-layer timing: python -m py_compile skills/development-harness/scripts/tests/test_hook_python_detection.py exits 0 (~0.1s); python -m unittest skills.development-harness.scripts.tests.test_hook_python_detection -v passes 5/5 (0.002s); python -m unittest discover skills/development-harness/scripts/tests passes 227/228 with 1 OS-specific skip (37.3s, up from 223 -- 5 new cases for unit_bugfix_001)"
          ],
          "issue_id": "ISSUE_001"
        },
        {
          "id": "unit_bugfix_002",
          "description": "ISSUE_002 fix: retire the Stop-hook-as-continuation-driver for Claude Code (Cursor hook stays unchanged). (a) Add new doc skills/development-harness/references/claude-code-continuation.md explaining why Cursor's followup_message protocol cannot port to Claude Code's one-shot stop_hook_active guard. (b) Rewrite skills/development-harness/templates/claude-code/hooks/continue-loop.py to a precondition-only shape: run the authority chain (flag, budget, blockers, open_questions, selector-vs-checkpoint agreement), print an advisory to stdout describing next_unit or the reason to stop, always exit 0; do not attempt exit-2 block-continue. (c) Add 'Claude Code: run under /loop' sections to skills/development-harness/commands/invoke.md and skills/development-harness/templates/workspace-commands/invoke-development-harness.md pointing Claude Code users at '/loop /invoke-development-harness' as the autonomous-run primitive; document that direct /invoke-development-harness runs exactly one unit per turn on Claude Code by protocol. (d) Update skills/development-harness/commands/create.md Phase 5 (Hook configuration) so Claude Code installs wire the hook as a precondition checker (not a continue-driver) and the generated .claude/settings.local.json reflects the new role. After this lands, PHASE_008 (Stop-hook fleet-awareness, units 034-036) needs to be re-scoped so its fleet-mode checks layer onto the precondition-checker shape, not the retired block-continue shape.",
          "acceptance_criteria": [
            "references/claude-code-continuation.md exists and names the one-shot stop_hook_active guard and Cursor's followup_message as the structural mismatch",
            "templates/claude-code/hooks/continue-loop.py contains no sys.exit(2) and no {'decision': 'block'} emission; authority chain remains but output is advisory-only",
            "commands/invoke.md and workspace-commands/invoke-development-harness.md both carry a 'Claude Code: run under /loop' section naming '/loop /invoke-development-harness' as the autonomous-run entry point",
            "commands/create.md Phase 5 describes the Claude Code hook as a precondition checker and does not describe block-continue semantics for Claude Code",
            "Cursor hook templates/hooks/continue-loop.py is unchanged (diff: 0 lines)"
          ],
          "validation_method": "docs-linter tests asserting each acceptance bullet; hook exit-code test for the rewritten Claude Code template (precondition-only shape)",
          "status": "completed",
          "validation_evidence": [
            "(a) skills/development-harness/references/claude-code-continuation.md (new 72-line doc): names the protocol mismatch (Cursor followup_message with no guard vs Claude Code's one-shot stop_hook_active + loop-guard floor), documents why the original Cursor port was structurally broken on Claude Code, lays out the two-track fix (Claude Code precondition-only + /loop, Cursor unchanged), and flags the PHASE_008 follow-up re-scoping that will layer fleet-mode checks onto the new shape.",
            "(b) skills/development-harness/templates/claude-code/hooks/continue-loop.py rewritten to precondition-only shape (~130 LOC). Module docstring explicitly records the role retirement (ISSUE_002) and links the reference doc. New _evaluate(cwd) helper runs the authority chain (fleet.mode guard, loop_budget, blockers, open_questions, selector+phase-graph presence, selector-vs-checkpoint agreement) and returns a single-line advisory string: 'proceed: <unit> in <phase>' when preconditions pass, 'stop: <reason>' otherwise. main() prints the advisory, calls _cleanup() (removes .invoke-active + .parallel-disabled + .parallel-failures.jsonl), and ALWAYS exits 0. No sys.exit(2). No {'decision': 'block'} JSON. The stop_hook_active input is read but unused -- in precondition mode there is nothing to guard against.",
            "(c) skills/development-harness/commands/invoke.md + skills/development-harness/templates/workspace-commands/invoke-development-harness.md: both gained a 'Claude Code: run under /loop' section at the top naming '/loop /invoke-development-harness' as the autonomous-run entry point, calling out 'one batch per turn' as the direct-invocation behavior on Claude Code by protocol, and explicitly reassuring Cursor users that their installs are unchanged. The long-form invoke.md also links claude-code-continuation.md for the full protocol explanation.",
            "(d) skills/development-harness/commands/create.md Phase 5 Claude Code hook configuration subsection: added a preamble describing the hook's new role as a precondition checker, naming the authority chain fields it runs, stating the hook 'always exits 0' and 'does NOT emit {\"decision\": \"block\"} or exit(2)', and linking claude-code-continuation.md. The hook command JSON stays the same shape (still uses $PY from unit_bugfix_001), only the surrounding role description changes.",
            "Cursor side unchanged: skills/development-harness/templates/hooks/continue-loop.py is diff-zero in this unit. Cursor's followup_message driver remains canonical for Cursor installs.",
            "Test updates: skills/development-harness/scripts/tests/test_continue_loop_claude.py was rewritten from the block-continue contract (exit 2 + decision:block JSON + .invoke-active preserved on continue) to the precondition-only contract (exit 0 + stdout advisory + .invoke-active cleaned up on every path). Final test count for this module: 9 cases across 4 classes. TestFleetModeGuard (4): dispatched + merging + idle + v1-missing-fleet behaviors. TestPreconditionCheckerReachesProceed (2): full-chain proceeds advisory, and stop_hook_active=True has no effect in precondition mode. TestFlagAbsentIsNoop (1): flag-absent silent no-op. TestNonIdleBudgetAndBlockers (2): loop_budget exhausted + blockers non-empty produce specific advisory strings.",
            "skills/development-harness/scripts/tests/test_claude_code_continuation_docs.py (new 180-line module, 12 cases across 3 classes): TestCreateDocClaudeCodeHookRole (4) pins that create.md Phase 5 Claude Code branch names 'precondition checker', points at /loop, links claude-code-continuation.md, and carries a 'does NOT emit' + 'always exits 0' negating callout so a future rewrite cannot silently re-install the block-continue driver. Section-slicing isolates the Claude Code subsection so Cursor wording cannot leak in. TestInvokeDocsLoopSection (4) pins the 'Claude Code: run under `/loop`' heading + the '/loop /invoke-development-harness' command-string + the 'one batch per turn' framing + the 'Cursor ... unchanged' reassurance across both invoke docs. TestClaudeCodeContinuationReferenceExists (4) pins the new reference doc's existence + the protocol-mismatch naming (stop_hook_active, followup_message) + the two-track fix (precondition-only, /loop) + the Cursor-unchanged claim.",
            "Pre-existing test_safety_rails.py TestHookStopClearsSafetyRails cases (2) still pass unchanged: the new hook's _cleanup() still sweeps .invoke-active + .parallel-disabled + .parallel-failures.jsonl in one pass, so the hook-stop cleanup contract is preserved even though the block-continue role is gone.",
            "ISSUE_002.json: status flipped from 'open' to 'resolved' with resolved timestamp 2026-04-20T10:15:00Z. state.json issues counter updated: total 2, open 0, resolved 2.",
            "Validation evidence with per-layer timing: python -m py_compile on all 3 edited/new Python files exits 0 (~0.1s); python -m unittest skills.development-harness.scripts.tests.test_continue_loop_claude -v passes 9/9 (0.7s); python -m unittest skills.development-harness.scripts.tests.test_claude_code_continuation_docs -v passes 12/12 (0.004s); python -m unittest discover skills/development-harness/scripts/tests passes 243/244 with 1 OS-specific skip (38.7s, up from 228 -- 16 new cases + 9 rewritten cases for unit_bugfix_002)."
          ],
          "issue_id": "ISSUE_002"
        },
        {
          "id": "unit_045",
          "description": "Add 'Parallel Execution Model' section to skills/development-harness/references/architecture.md: worktree-per-unit, orchestrator/sub-agent boundary, frontier+overlap check, dispatch-wait-merge lifecycle, conflict strategies, when to enable/not",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/references/architecture.md: added a new top-level 'Parallel Execution Model' section (inserted between 'Batch Semantics' and 'Git Integration') with nine sub-sections covering every acceptance bullet plus the implicit prerequisites. (1) 'When to enable parallelism' names the config.execution_mode.parallelism knobs with a concrete JSON snippet + the three-way gate for unit candidacy (parallel_safe + depends_on satisfied + non-empty touches_paths when require_touches_paths). (2) 'Worktree-per-unit layout' pins the on-disk .harness/worktrees/<batch_id>/<unit_id>/ path + harness/<batch_id>/<unit_id> branch pattern + the WORKTREE_UNIT.json sentinel as the sub-agent's scope binding. (3) 'Orchestrator / sub-agent boundary' names the Agent(subagent_type: \"harness-unit\") call + the worktree-confined allowlist + the no-push/merge/rebase rule + the 'sub-agent self-report never trusted for blast radius' invariant. (4) 'Frontier + overlap check' names select_next_unit.py --frontier + compute_parallel_batch.py + the five packing constraints (parallel_safe, require_touches_paths, max_concurrent_units, allow_cross_phase, overlap matrix via fnmatch). (5) 'Dispatch -> wait -> merge lifecycle' walks the idle -> dispatched -> merging -> idle state progression, pins that scope-check runs BEFORE the merge attempt, and names the post-merge cleanup + stop-hook-never-sees-partial-fleet invariant. (6) 'Conflict strategies' contrasts abort_batch (default) vs serialize_conflicted with downstream consequences + cross-links to phase-contract.md's scope-violation always-on rule. (7) 'Merge serialization via .harness/.lock' names the O_EXCL mutex + stale-lock takeover mechanism. (8) 'Safety rails -- session kill switch' names safety_rails.py + .parallel-disabled + the 2-failure threshold + session-scoped clearing. (9) 'Observability' names all four .harness/logs/<batch_id>/ artifacts (batch.json, <unit_id>.md, merge.log, validation.log) + the best-effort semantics + the /harness-state rendering integration. Concludes with a forward link to parallel-execution.md for the full dispatch lifecycle + conflict-strategy catalogue (unit_047)",
            "skills/development-harness/scripts/tests/test_architecture_parallel_section.py: new 160-line test module with 1 class (TestArchitectureParallelExecutionSection) and 10 cases. Every acceptance bullet has a dedicated test pinning the sub-section heading + all substantive tokens (config paths, file paths, function names, state values, invariant phrases). test_section_places_before_git_integration is a position-order assertion that catches duplicate or misplaced sections by index comparison. Section-slicing helper guards against cross-section contamination -- assertions only match within the new section. The dispatch-wait-merge lifecycle test was initially too strict (expected '\"dispatched\"' with literal double-quotes) but caught by the first run and relaxed to accept either `idle`/`\"idle\"` framing for stylistic flexibility",
            "Grounding: every claim in the new section is verified against a specific file in the repo. dispatch_batch.py has the WORKTREE_UNIT.json seeding + atomic teardown. merge_batch.py has the pre-merge scope check + lock acquisition. compute_parallel_batch.py has the greedy-pack + overlap matrix. safety_rails.py has the kill-switch write. _MergeLock has the stale-lock takeover. The section is a true summary of what the code actually does, not aspirational documentation",
            "Validation evidence with per-layer timing: python -m py_compile skills/development-harness/scripts/tests/test_architecture_parallel_section.py exits 0 (~0.1s); python -m unittest skills.development-harness.scripts.tests.test_architecture_parallel_section -v passes 10/10 (0.003s); python -m unittest discover skills/development-harness/scripts/tests passes 253/254 with 1 OS-specific skip (37.8s, up from 244 -- 10 new cases for unit_045)"
          ]
        },
        {
          "id": "unit_046",
          "description": "Update skills/development-harness/references/phase-contract.md to document required depends_on, touches_paths (when parallel_safe: true), and parallel_safe; add 'Decomposing a phase for parallelism' subsection",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/references/phase-contract.md Units of Work table: extended from 4 fields (id, description, acceptance criteria, validation method) to 7 by adding depends_on, parallel_safe, and touches_paths rows. Added a new 'Required' column that makes the when-required conditional explicit -- id/description/acceptance/validation are 'always'; depends_on is 'always (empty list allowed)'; parallel_safe is 'always (default false)'; touches_paths is 'required when parallel_safe: true (under default config)'. Each row names the enforcing script: compute_frontier in select_next_unit.py gates depends_on, compute_parallel_batch.py consumes parallel_safe, and the merge_batch.py scope check consumes touches_paths. The touches_paths row cross-links to the existing Scope-Violation Enforcement Policy section (unit_039) so readers chase the trust-boundary rationale",
            "skills/development-harness/references/phase-contract.md: added new top-level 'Decomposing a phase for parallelism' section after the Scope-Violation Enforcement Policy. Five numbered steps walk phase authors through the decomposition recipe: Step 1 (draw the dependency graph -- distinguish real depends_on from spurious logical-ordering preferences, with concrete examples on both sides), Step 2 (identify each unit's blast radius for touches_paths, with the 'one directory per unit' heuristic and the 'shared test utilities are the usual hazard' warning), Step 3 (check for overlap via the fnmatch-based overlap matrix in compute_parallel_batch.py, with the 'narrow globs, don't disable the check' guardrail), Step 4 (set parallel_safe as a 'declaration of independence, not a performance hint' with four explicit criteria and the 'when in doubt, leave it false' default), Step 5 (dry-run the batch via select_next_unit.py --frontier piped into compute_parallel_batch.py to surface capacity/overlap/cross-phase rejections before merge time). Closes with an Anti-patterns subsection naming three real anti-patterns observed in dogfooding: the 'set up everything' unit pattern, parallel_safe as a speed signal, and globs that cover shared files",
            "skills/development-harness/scripts/tests/test_phase_contract_parallelism_fields.py: new 170-line test module with 3 classes and 11 cases. TestUnitsOfWorkTableCarriesParallelismFields (4): table gains a Required column (catches a future revert to the pre-PHASE_011 4-column table), depends_on row names compute_frontier, parallel_safe row names compute_parallel_batch.py + describes the in-tree-fast-path fallback for parallel_safe=false, touches_paths row pins the 'required when parallel_safe: true' conditional + cross-links to the Scope-Violation Enforcement Policy. TestDecompositionSubsection (6): section exists, carries Step 1..Step 5 sub-headings (catches partial deletion), Step 1 mentions 'dependency graph' + the 'NOT real' counterexamples label, Step 2-3 mentions 'blast radius' + 'one directory per unit' heuristic + overlap_matrix + touches_overlap exclusion reason string, Step 4 mentions 'declaration of independence' + 'performance hint' contrast, Step 5 + anti-patterns mention the dry-run command string + Anti-patterns sub-heading. TestDecompositionSectionPlacedAfterScopePolicy (1): position-order assertion that Scope-Violation Enforcement Policy precedes Decomposing a phase for parallelism, so readers encounter the trust-boundary rationale before the how-to guidance",
            "Grounding: every section claim is verifiable against runtime code. compute_frontier (select_next_unit.py L60-125 depending on version) is the depends_on gate. compute_parallel_batch.py L136 reads require_touches_paths; L148 enforces the excluded 'not_parallel_safe' reason for units lacking touches_paths under require_touches_paths=true; L180-220 runs the overlap matrix. merge_batch.py L233-252 _scope_violations is the merge-time enforcer. The Decomposition section's Step 5 dry-run command is the actual pipeline from commands/invoke.md Step 4",
            "Validation evidence with per-layer timing: python -m py_compile skills/development-harness/scripts/tests/test_phase_contract_parallelism_fields.py exits 0 (~0.1s); python -m unittest skills.development-harness.scripts.tests.test_phase_contract_parallelism_fields -v passes 11/11 (0.003s); python -m unittest discover skills/development-harness/scripts/tests passes 264/265 with 1 OS-specific skip (37.6s, up from 254 -- 11 new cases for unit_046)"
          ]
        },
        {
          "id": "unit_047",
          "description": "New skills/development-harness/references/parallel-execution.md: dispatch lifecycle, overlap-matrix algorithm, merge-order rationale, failure-mode catalog, recovery procedures, readiness checklist",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/references/parallel-execution.md (new, ~240 lines) consolidates the operator-facing deep-dive that architecture.md (unit_045) and phase-contract.md (unit_046) point forward to. Seven top-level numbered sections: (1) Dispatch lifecycle -- ASCII flow diagram walking idle -> dispatched -> merging -> idle with exact invariants (stop-hook-never-sees-non-idle at turn-end + /sync recovery path). (2) Overlap-matrix algorithm -- the full greedy-packer pseudocode plus the four gates (parallel_safe + touches_paths requirement, cross-phase silent-defer, capacity_cap, pairwise overlap via fnmatch) plus the verbatim-exclusion-reason table. (3) Merge-order rationale -- explains why the order is stable-by-design (determinism beats throughput, per-unit scope checks make smarter orders moot, real conflicts are rare when touches_paths is right) and pins the --no-ff mode + first-parent log attribution invariant. (4) Failure-mode catalog -- 5-row table covering scope_violation, merge_conflict, post_merge_validation_failed, infrastructure, ambiguity; each row names source, conflict_strategy effect, and whether it counts toward the kill-switch; explicit cross-link to safety_rails.py COUNTED_CATEGORIES. (5) Recovery procedures -- four scenarios (mid-dispatch crash, mid-merge crash, kill-switch tripped, scope-violation dispute) with numbered step-by-step instructions and the explicit 'never force-delete the lock' warning. (6) Readiness checklist -- 8 markdown checkboxes gating 'should you enable parallelism'. (7) Observability quick reference -- names all four .harness/logs/<batch_id>/ artifacts + pins the exact Batch Timings format string",
            "Doc/code consistency fix bundled in this unit: earlier doc work in unit_045 and unit_046 referenced the overlap exclusion reason as 'touches_overlap' but the actual string compute_parallel_batch.py L170 emits is 'path_overlap_with:<id>'. Discovery came from grep-ing the code while writing parallel-execution.md. Fixed in architecture.md L133 and phase-contract.md L138 + L161 to match the code; the corresponding regression tests (test_architecture_parallel_section.py + test_phase_contract_parallelism_fields.py) were updated to assert the correct string. parallel-execution.md itself uses 'path_overlap_with' everywhere from the start and its test module contains an explicit negative assertion that 'touches_overlap' is NOT present (guards against a future edit reintroducing the typo)",
            "skills/development-harness/scripts/tests/test_parallel_execution_reference.py: new 180-line test module with 3 classes and 10 cases. TestParallelExecutionDocStructure (7) asserts each numbered section exists + carries the substantive anchors (all three fleet.mode states, all three exclusion-reason strings, all five conflict categories, the Batch Timings format string exactly, markdown-checkbox syntax in the readiness checklist, etc.). TestParallelExecutionDocCrossLinks (2) asserts the new doc links both architecture.md and phase-contract.md AND that architecture.md's forward-link to parallel-execution.md survives. TestExclusionReasonConsistencyWithCode (1) pins the path_overlap_with string AND explicitly forbids reintroduction of 'touches_overlap' -- a regression guard specifically for the discrepancy caught in this unit",
            "Validation evidence with per-layer timing: python -m py_compile skills/development-harness/scripts/tests/test_parallel_execution_reference.py exits 0 (~0.1s); python -m unittest skills.development-harness.scripts.tests.test_parallel_execution_reference -v passes 10/10 (0.003s); python -m unittest discover skills/development-harness/scripts/tests passes 274/275 with 1 OS-specific skip (37.6s, up from 265 -- 10 new cases for unit_047)"
          ]
        },
        {
          "id": "unit_048",
          "description": "Update skills/development-harness/commands/create.md Phase 2 with two structured questions: (1) Enable parallel unit execution? (y/n, default n) -> config.execution_mode.parallelism.enabled; (2) Break-on-schema-bump vs migrate? (break/migrate, default break) -> config.execution_mode.versioning.break_on_schema_bump. Update Phase 4 to instruct proposing touches_paths per unit and setting parallel_safe: false when blast radius is unknown",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/commands/create.md Phase 2 (Ask Questions): new 'Execution Mode' category added after Quality with a 'use exact wording' preamble and two structured questions. Q1 '`Enable parallel unit execution? (y/n, default n)`' maps to config.execution_mode.parallelism.enabled with full y->true / n->false mapping + the four sub-fields that must be written alongside the enabled flag (max_concurrent_units default 3, conflict_strategy default abort_batch, require_touches_paths default true, allow_cross_phase default false) + an on-yes-answer pointer at references/parallel-execution.md § 'Readiness checklist'. Q2 '`Break-on-schema-bump vs migrate? (break/migrate, default break)`' maps to config.execution_mode.versioning.break_on_schema_bump with the break->true (safe default, requires /create-development-harness regeneration on schema bump) vs migrate->false (attempts in-place migration) semantics called out so the generated config is self-documenting",
            "skills/development-harness/commands/create.md Phase 4 Step 2: unit-field bullet list grew from 4 fields (id, description, acceptance criteria, validation method) to 7 by adding depends_on (always required, empty list allowed), parallel_safe (with the explicit 'Default to false when blast radius is unknown' guidance the acceptance criterion required), and touches_paths (with the 'Propose this per unit' instruction + 'narrower globs' preference + 'required when parallel_safe: true' conditional). Step 3 'Determine dependencies' disambiguated as phase-level (to differentiate from the new unit-level depends_on). New 'Parallelism-by-default' paragraph below the numbered list instructs the agent to propose parallel_safe: true aggressively when Phase 2 turned parallelism on, paired with the exact dry-run pipeline `select_next_unit.py --frontier | compute_parallel_batch.py --input - --config .harness/config.json` and the 'when in doubt, leave parallel_safe: false' safety net",
            "skills/development-harness/scripts/tests/test_create_bootstrap_parallelism_questions.py: new 190-line test module with 2 classes and 11 cases. TestPhase2ExecutionModeQuestions (7) pins the Execution Mode category heading + both questions verbatim (backtick-quoted) + their answer-to-value mappings (true/false/break/migrate) + both config paths (config.execution_mode.parallelism.enabled and config.execution_mode.versioning.break_on_schema_bump) + the four sub-fields the parallelism question must instruct writing + the parallel-execution.md + Readiness-checklist cross-link + the /create-development-harness recovery path for schema-bump breaks + the 'use exact wording' preamble. TestPhase4UnitFieldsAddParallelismTriple (4) pins the depends_on/parallel_safe/touches_paths additions + the 'blast radius' + 'Default to false' guidance wording (the exact phrases in the acceptance criterion) + the 'Propose this' + 'narrower globs' touches_paths instruction + the Parallelism-by-default subsection with the full dry-run command + the path_overlap_with cross-link. Section-slicing helper (_slice) guards assertions against leaking across phase sections in the doc",
            "Grounding: every claim in the edits is verified against the downstream config schema + runtime code. config.execution_mode.parallelism.enabled is the exact field compute_parallel_batch.py:101 reads. config.execution_mode.versioning.break_on_schema_bump is the field validate_harness.py's schema-check path reads (verified via the schema definition). depends_on is the field compute_frontier in select_next_unit.py reads. parallel_safe + touches_paths are the fields compute_parallel_batch.py:145-148 reads. The dry-run pipeline command in the 'Parallelism-by-default' paragraph is the exact invocation commands/invoke.md Step 4 uses",
            "Validation evidence with per-layer timing: python -m py_compile skills/development-harness/scripts/tests/test_create_bootstrap_parallelism_questions.py exits 0 (~0.1s); python -m unittest skills.development-harness.scripts.tests.test_create_bootstrap_parallelism_questions -v passes 11/11 (0.004s); python -m unittest discover skills/development-harness/scripts/tests passes 285/286 with 1 OS-specific skip (41.6s, up from 275 -- 11 new cases for unit_048)"
          ]
        },
        {
          "id": "unit_049",
          "description": "Update skills/development-harness/SKILL.md with 'Parallel execution (optional)' subsection and 'Version upgrades' note: re-run /create-development-harness on schema_version changes; no migration script provided",
          "status": "pending",
          "validation_evidence": []
        },
        {
          "id": "unit_050",
          "description": "Cross-check pass: every new field, script, rule, and command introduced by this roadmap is referenced in at least one doc under skills/development-harness/references/",
          "status": "pending",
          "validation_evidence": []
        }
      ]
    },
    {
      "id": "PHASE_012",
      "slug": "release-readiness",
      "status": "completed",
      "depends_on": ["PHASE_001"],
      "started": "2026-04-20T12:10:00Z",
      "completed": "2026-04-20T13:20:00Z",
      "units": [
        {
          "id": "unit_051",
          "description": "Add parallelism readiness checklist to skills/development-harness/references/parallel-execution.md: touches_paths on every parallelism-eligible unit, no shared-aggregator units, CI handles multi-commit pushes",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/references/parallel-execution.md § 6 'Readiness checklist': extended the pre-unit_047 flat list of 8 items with the three PHASE_012 acceptance bullets and split the section into two sub-headings -- 'Unit-declaration readiness' (4 items covering independence, touches_paths, glob disjointness, shared-aggregator avoidance) and 'Runtime & infrastructure readiness' (5 items covering post-merge validator, /harness-state dry-run, git worktree support, O_EXCL filesystem support, CI multi-commit-push capability, dogfooded throwaway fixture). The 3 new unit_051 additions are bolded so readers can distinguish them from the pre-existing baseline",
            "Unit_051 bullet 1 'touches_paths on every parallelism-eligible unit' strengthens the pre-unit_047 question by adding actionable context: names the 'not_parallel_safe' exclusion reason that hits when require_touches_paths: true (the default) meets an empty declaration; prescribes the 'walk every phase doc's Units-of-Work table' audit; cross-links to phase-contract.md § 'Scope-Violation Enforcement Policy' so readers chase the trust-boundary rationale",
            "Unit_051 bullet 2 'no shared-aggregator units' is the subtle one -- the checklist defines aggregator as 'a unit whose job is to modify one file everyone else depends on' with three concrete cross-stack examples (central router, single index.ts re-exporter, top-level __init__.py). The collision failure mode is named explicitly ('collide every time two sibling units also need to add to the aggregator's file') and two concrete remediations are offered: absorb the aggregator work into dependents (keeps touches_paths disjoint) OR serialize via depends_on (only one updates the aggregator at a time). This turns the check from 'avoid this smell' into 'here's the fix if you find it'",
            "Unit_051 bullet 3 'CI handles multi-commit pushes' names the actual commit-message template ('harness: merge <unit_id>'), the post-merge-loop push pattern (all commits land back-to-back then the orchestrator pushes them together), and two concrete CI anti-patterns (per-commit hooks that run a full test matrix per commit in rapid succession, rate-limiting status checks) + a verification step (inspect pipeline trigger semantics, confirm 'push with N commits' produces exactly one pipeline run or N bounded runs) + a concrete remediation ('keep parallelism off until the pipeline is fixed')",
            "skills/development-harness/scripts/tests/test_parallel_execution_readiness_checklist.py: new 200-line test module with 5 classes and 13 cases. TestReadinessChecklistHasUnitDeclarationSubsection (2): both sub-headings exist. TestReadinessChecklistTouchesPathsCheck (3): bolded + exact wording + names not_parallel_safe + require_touches_paths + cross-links to Scope-Violation Enforcement Policy. TestReadinessChecklistSharedAggregatorCheck (3): bolded + exact wording + aggregator definition + 3 concrete examples (router, index.ts, __init__.py) + proposes 2 remediations (absorb, depends_on). TestReadinessChecklistMultiCommitCICheck (4): bolded + exact wording + names 'harness: merge' commit-message template + two CI anti-patterns (per-commit hooks, rate-limit) + 'keep parallelism off until' remediation. TestReadinessChecklistPreservesPreUnit051Items (1): all 7 pre-unit_051 items survive (guards against a careless rewrite that replaces the list with only the 3 new items)",
            "Grounding: every claim is verified against runtime code + prior units. 'not_parallel_safe' and 'require_touches_paths' come directly from compute_parallel_batch.py's exclusion-reason strings. 'harness: merge <unit_id>' matches the merge commit template in merge_batch.py:182. 'overlap matrix' ties into unit_047's deep-dive section 2. The cross-link to phase-contract.md § 'Scope-Violation Enforcement Policy' preserves the trust-boundary reading order (policy-first, then checklist)",
            "Validation evidence with per-layer timing: python -m py_compile skills/development-harness/scripts/tests/test_parallel_execution_readiness_checklist.py exits 0 (~0.1s); python -m unittest skills.development-harness.scripts.tests.test_parallel_execution_readiness_checklist -v passes 13/13 (0.004s); python -m unittest discover skills/development-harness/scripts/tests passes 303/304 with 1 OS-specific skip (38.3s, up from 291 -- 13 new cases for unit_051)"
          ]
        },
        {
          "id": "unit_052",
          "description": "Add 'Version upgrades' note to skills/development-harness/SKILL.md: re-run /create-development-harness when schema_version changes; no migration script is provided by design",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/SKILL.md: added a new '## Version upgrades' section between 'Python Detection' and 'Key Principle'. Three numbered steps walk the upgrade path: (1) Re-run /create-development-harness to regenerate .harness/ artifacts at the new schema version; (2) ROADMAP.md and PHASES/*.md are preserved -- the recreate flow reads them and rebuilds the harness around them (this is the claim that makes the upgrade path palatable: real product/planning work is never regenerated from scratch); (3) Harness-owned files that do get regenerated (state.json, phase-graph.json, checkpoint.md, manifest.json, scripts, workspace commands, hook+rule files) are called out explicitly so users with hand-edits know those edits will be lost",
            "Follows the steps with the load-bearing paragraph: 'No migration script is provided by design.' verbatim (per the phase doc acceptance criterion). The cost argument is named explicitly -- a reliable general-purpose migration covering every schema-version delta across every downstream file exceeds the cost of regenerating with ROADMAP + PHASES as preserved inputs. Cross-links to config.execution_mode.versioning.break_on_schema_bump (the default true set during Phase 2 of /create-development-harness) so users who want the in-place migration path know which knob to flip (break_on_schema_bump: false) AND know upfront that the harness ships no migration tooling to back that choice up",
            "skills/development-harness/scripts/tests/test_skill_md_version_upgrades.py: new 150-line test module with 3 classes and 8 cases. TestVersionUpgradesSectionPresent (2) pins heading presence + position-order assertion that the section precedes 'Key Principle' (so a user hitting the schema-mismatch error sees the upgrade path before the closing philosophical summary). TestVersionUpgradesSubstantiveClaims (5) pins each of the 5 acceptance-criterion tokens: /create-development-harness action + 'Re-run' verb, ROADMAP.md + PHASES/*.md + 'preserved' claim, verbatim 'no migration script is provided by design' lowercase match, cost argument presence, break_on_schema_bump config knob mention + 'true' default. TestVersionUpgradesListsRegeneratedFiles (1) pins that the 4 core regenerated files (state.json, phase-graph.json, checkpoint.md, manifest.json) are all named so users with hand-edits aren't surprised",
            "Grounding: every claim is verified against runtime code + prior units. validate_harness.py L84-107 enforces the schema_version check that triggers the 'Re-run /create-development-harness' flow. config.execution_mode.versioning.break_on_schema_bump was added in unit_048 (PHASE_011) with 'break -> true' as the default; this section lands the companion user-facing documentation. The 'ROADMAP.md + PHASES/*.md preserved' claim matches commands/create.md Phase 3 + Phase 4 behavior (which read from these files rather than regenerating them)",
            "Validation evidence with per-layer timing: python -m py_compile skills/development-harness/scripts/tests/test_skill_md_version_upgrades.py exits 0 (~0.1s); python -m unittest skills.development-harness.scripts.tests.test_skill_md_version_upgrades -v passes 8/8 (0.002s); python -m unittest discover skills/development-harness/scripts/tests passes 311/312 with 1 OS-specific skip (38.2s, up from 304 -- 8 new cases for unit_052)"
          ]
        },
        {
          "id": "unit_053",
          "description": "Update skills/development-harness/commands/update.md (and the workspace template) to detect schema_version mismatch between installed harness and skill; emit a pointer to the recreate flow; test coverage via a v1 fixture",
          "status": "completed",
          "validation_evidence": [
            "skills/development-harness/commands/update.md: added a new Step 0 'Schema Version Precheck' at the top of the steps list (before 'Read All Harness Files') that invokes validate_harness.py and branches on its output. Exit 0 proceeds to normal update flow. Exit 1 with '/create-development-harness' in the error output surfaces a verbatim advisory to the user pointing at the recreate flow + preserving ROADMAP.md + PHASES/*.md + cross-linking SKILL.md § Version upgrades + the policy statement 'no migration script is provided by design'. Also explicitly forbids in-place migration ('Do NOT attempt in-place migration. Do NOT enter Plan Mode.'). Exit 1 without the pointer is a different structural problem and reports errors generically. The pre-existing 'Schema migration' row in the Categorize-the-Change table was removed + replaced with a blockquote noting that schema_version bumps are NOT a category (they trigger the Step 0 rejection). The pre-existing Step 10 Schema Migration section was rewritten to 'Schema Migration (not supported)' with the refusal language + redirect instruction",
            "skills/development-harness/templates/workspace-commands/update-development-harness.md: mirrored the edits for the Cursor/workspace variant. Uses 'Step 0.5: Schema Version Precheck' (the template already had a 'Step 0: Resolve tool paths' from earlier work, so the precheck is numbered 0.5 to sit between tool-path resolution and the main procedure). Body is substantively identical -- same validate_harness invocation, same advisory text, same 'Do NOT attempt' forbidding, same 'no migration script is provided by design' policy statement, same SKILL.md cross-link, same Schema Migration (not supported) section. Ensures Cursor and Claude Code installs produce identical user-facing behavior on a schema mismatch",
            "skills/development-harness/scripts/tests/test_update_command.py: new 230-line test module with 2 classes and 9 cases. TestV1FixtureTripsSchemaMismatch (2) seeds a v1-shaped harness workspace (all 4 JSON files at schema_version '1.0') and invokes validate_harness.py via subprocess exactly as Step 0/0.5 instructs. test_v1_fixture_produces_recreate_pointer asserts returncode == 1 + valid flag false + '/create-development-harness' in joined error output (the end-to-end user-flow assertion). test_v1_all_four_json_files_referenced_in_errors asserts all 4 filenames appear in the errors so the update doc can tell the user exactly which files will be regenerated. TestUpdateDocsCarryPrecheckContract (7) pins the doc-shape contract across BOTH update.md and workspace-commands/update-development-harness.md: Schema Version Precheck section exists, validate_harness.py is named as the precheck tool, /create-development-harness + 'older schema' pointer language present, verbatim 'no migration script is provided by design' (lowercase match), SKILL.md § Version upgrades cross-link, 'Do NOT attempt' forbidding present, AND a long-doc-specific assertion that the pre-unit_053 'Schema migration' table row is gone (catches regression)",
            "Grounding: every test claim matches verified runtime behavior. validate_harness.py's v1 rejection is pinned by test_validate_harness.py (PHASE_001 unit_006). The joined-errors substring contract ('/create-development-harness' + all 4 JSON filenames) is the exact behavior that file's tests confirm. This unit's tests wrap that validator behavior in a /update-flow labeled context so the doc-shape + user-flow contract is verified end to end",
            "Validation evidence with per-layer timing: python -m py_compile skills/development-harness/scripts/tests/test_update_command.py exits 0 (~0.1s); python -m unittest skills.development-harness.scripts.tests.test_update_command -v passes 9/9 (0.2s); python -m unittest discover skills/development-harness/scripts/tests passes 320/321 with 1 OS-specific skip (38.3s, up from 312 -- 9 new cases for unit_053). PHASE_012 closes: all three units (051 + 052 + 053) have validation_evidence; phase marked completed"
          ]
        }
      ]
    },
    {
      "id": "PHASE_013",
      "slug": "harness-self-test",
      "status": "pending",
      "depends_on": ["PHASE_007", "PHASE_008", "PHASE_009", "PHASE_010", "PHASE_011", "PHASE_012"],
      "started": null,
      "completed": null,
      "units": [
        {
          "id": "unit_054",
          "description": "Create fixture at skills/development-harness/scripts/tests/fixtures/self-test/: throwaway workspace with a 2-phase / 6-unit mini-CRUD roadmap designed to exercise a batch >= 2, one merge conflict, and one scope violation",
          "status": "pending",
          "validation_evidence": []
        },
        {
          "id": "unit_055",
          "description": "Run /create-development-harness on the fixture; populate touches_paths on every unit; enable parallelism; capture the generated phase-graph and config for the record",
          "status": "pending",
          "validation_evidence": []
        },
        {
          "id": "unit_056",
          "description": "Run /invoke-development-harness iteratively until all fixture phases complete; ensure the run exercises batch >= 2, one merge conflict, and one scope violation",
          "status": "pending",
          "validation_evidence": []
        },
        {
          "id": "unit_057",
          "description": "Capture execution traces; produce a data table comparing wall-clock with parallelism enabled vs a sequential baseline; verify no orphaned worktrees/branches after completion",
          "status": "pending",
          "validation_evidence": []
        },
        {
          "id": "unit_058",
          "description": "Write post-mortem under skills/development-harness/scripts/tests/fixtures/self-test/POST-MORTEM.md and link it from skills/development-harness/references/parallel-execution.md",
          "status": "pending",
          "validation_evidence": []
        }
      ]
    }
  ]
}
